@echo off
rem ─────────────────────────────────────────────────────────────────────────────
rem 백년지도(3번 · 100yearmap.com) 세션을 연다.
rem
rem ⚠ 2번의 `_이어서.cmd` 와 **다른 파일**이다. 같은 저장소를 쓰지만 세션이 다르다.
rem    그 파일은 2번 소유라 건드리지 않는다.
rem
rem   · 이어받을 대화가 있으면 잇는다(`--continue`).
rem   · 없으면 **첫 지시와 함께** 새로 시작한다.
rem     (`--continue` 는 이어갈 대화가 없으면 그냥 끝난다 — 창만 뜨고 닫힌다)
rem
rem ⚠ 전역 설정(`~/.claude/settings.json`)은 **세션이 시작할 때** 읽는다.
rem    이미 떠 있던 창에는 안 붙는다. 설정을 바꾸면 이 입구로 새로 열어야 한다.
rem ─────────────────────────────────────────────────────────────────────────────
title 백년지도 100yearmap
cd "%~dp0"
claude --continue || claude "너는 3번(백년지도 · 100yearmap.com) 세션이다. 먼저 `C:\Users\USER\Desktop\0804_3\03_인계-현재상태.md`(가장 최근 날짜의 `0804_3` 계열 폴더)를 읽어라. 그 다음 `docs/세션간-메모.md` 끝부분에서 1번·2번이 남긴 것을 확인해라. `CLAUDE.md`·`docs/작업지시서.md`·`docs/인계-현재상태.md` 세 파일은 2번 소유라 읽기만 한다. 그리고 인계 문서의 「다음에 할 일」 순서대로 이어서 작업해라."
