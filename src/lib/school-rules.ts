/**
 * 백년지도 **학교 자료 규칙 한 곳** — 수집기와 검사가 **같이 불러 쓴다.**
 *
 * ## 🔴 왜 만드나 (2026-08-07 · 2번 지시)
 *
 *   같은 규칙이 **다섯 곳에 손으로 적혀** 있었다.
 *
 *   ```
 *   collect-alimi-dropout.mjs      최소재학생 = 30
 *   collect-alimi-career.mjs       최소졸업자 = 30
 *   collect-kosis-voc-series.mjs   최소분모   = 30
 *   check-100yearmap-launch.mjs    작은분모   = 30
 *   collect-alimi-class-size.mjs   (방송통신 제외 규칙이 따로 적혀 있었다)
 *   ```
 *
 *   주석에 「하나를 바꾸면 넷을 같이 본다」고 적어 뒀는데, **그건 규칙이 아니라 부탁이다.**
 *   다음 수집기를 만드는 사람이 그 주석을 안 읽으면 그대로 어긋난다.
 *   실제로 오늘 수집기를 셋 더 만들면서 **매번 30을 손으로 다시 적었다.**
 *
 *   ⭐ 5번이 오늘 같은 것에 걸렸다 — *「규칙을 한 곳에 둬도 **다음 스크립트가 안 부를 수** 있다」*.
 *     그래서 **한 곳에 두는 것으로 끝내지 않고, 안 부르면 잡히는 검사**를 같이 둔다
 *     (`scripts/check-school-rules.mjs`).
 *
 * ⛔ 여기 값을 바꾸면 **수집기를 다시 돌려야** 자료에 반영된다. 지면만 다시 빌드하면 안 바뀐다.
 */

/**
 * 🔴 **분모가 이보다 작으면 비율을 내지 않는다.**
 *
 *   30명이면 한 사람이 3.3%p 다. 그 아래로는 **한 사람이 전국 평균과의 차이보다 크게 흔든다.**
 *
 *   실제로 겪은 것들 —
 *   ```
 *   상동고등학교   재학  3명 · 떠남 3명  →  「학업중단률 100%」가 나갈 뻔했다
 *   예술 계열      취업 대상 9명        →  「취업률 11.1%」는 「아홉 중 하나」다
 *   대학 10곳      취업대상자 0명       →  0/0 이 「전국 꼴찌」로 나갔었다
 *   ```
 *
 *   ⛔ 그때는 **비율 대신 실제 사람 수**로 말한다.
 *     「19명 가운데 3명」이 「15.8%」보다 정직하고 알아듣기도 쉽다.
 */
export const 최소분모 = 30;

/** 비율을 내도 되는 분모인가 */
export const 비율낼수있나 = (분모: number | null | undefined): boolean =>
  typeof 분모 === 'number' && Number.isFinite(분모) && 분모 >= 최소분모;

/** 왜 비율을 안 냈는지 — 지면이 그대로 읽어 「없다」가 아니라 「못 낸다」로 말하게 한다 */
export const 비율못낸까닭 = (분모: number | null | undefined, 이름 = '분모'): string =>
  분모 == null
    ? `${이름} 수치가 없다`
    : `${이름}가 ${분모}명이라 비율이 뜻을 잃는다`;

/**
 * 🔴 **우리 고교 지면에 없는 갈래** — 「못 이었다」 셈에 넣지 않는다.
 *
 *   학교알리미 응답에는 방송통신고·특수학교·각종학교가 섞여 온다.
 *   그것을 「못 이은 학교」로 세면 못 이은 수가 8 에서 192 로 부풀어
 *   **「많이 놓쳤다」로 잘못 읽힌다.** 갈래를 갈라 세는 것이 정직하다.
 */
export const 방송통신인가 = (학교이름: string | null | undefined): boolean =>
  /방송통신/.test(String(학교이름 ?? ''));

/** 고교 갈래가 안 적힌 행 — 특수학교·각종학교다. 우리 지면에 없다 */
export const 우리갈래인가 = (고교유형: string | null | undefined): boolean =>
  Boolean(String(고교유형 ?? '').trim());

/**
 * 🔴 **못 이으면 버린다.** 짐작해 붙이지 않는다.
 *
 *   학교알리미 코드(`S000003540`)와 우리 NEIS 코드(`9010438`)는 다르다.
 *   **이름 + 시도**로 잇는다. 그 둘로 우리 2,525장은 전부 유일하다(실측).
 *
 *   ⚠ 저쪽 지역칸이 비어 있는 행이 있다. 그때만, 그리고 **이름이 양쪽 모두에서 하나뿐일 때만**
 *     이름으로 잇는다. 같은 이름이 둘씩이면 못 이은 채로 둔다 —
 *     억지로 붙이면 **남의 학교 숫자가 그 학교 지면에 나간다.**
 */
export const 잇는열쇠 = (이름: string | null | undefined, 시도: string | null | undefined): string =>
  `${String(이름 ?? '').trim()}|${시도 ?? ''}`;
