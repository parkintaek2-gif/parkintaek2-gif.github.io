/**
 * 백년지도가 파는 것의 **값과 다음 칸** — 한 곳에 둔다.
 *
 * ## 🔴 왜 (2026-08-08 11:0x · 2번 라이브 실측)
 *
 *   ```
 *   학교 지면 표본 25장 중 지역 한 벌로 가는 링크    0장
 *   파는 지면(노원구) — 9,900원 표기 ⛔ 없다 · 사는 단추 ⛔ 없다
 *   ```
 *
 *   *「258장을 다섯 시간 만에 세운 것은 큰 일인데, 지금 상태로는 손님이 값을 모르고,
 *     알아도 살 데가 없고, 애초에 그 지면에 닿을 길이 없다」*
 *
 * ## ⛔ 여기서 하지 않는 것
 *
 *   **결제 이야기를 하지 않는다.** 지금 필요한 것은 **값과 다음 칸**뿐이다.
 *   결제 수단이 붙기 전에 「결제하기」를 놓으면 누른 사람이 빈 화면을 본다.
 *   그래서 다음 칸은 **「곧 엽니다 · 알림 받기」**다.
 *
 * ## ⚠ 값을 지면에 손으로 적지 않는다
 *
 *   9,900 을 지면마다 적어 두면 값이 바뀔 때 **일부만 고쳐져 조용히 어긋난다.**
 *   `/work` 의 이름을 주소에 안 박아 둔 덕에 글자만 갈아 끼웠던 것과 같은 이치다.
 *
 * ⛔ **무료 지면에는 값을 띄우지 않는다.** 9곳 이하는 무료다 —
 *   거기 9,900 이 뜨면 그건 거짓말이 된다(2번 확인 항목).
 */

/**
 * 🔴🔴 **PG 하나에 묶는다** — 손으로 기억하지 않는다 (2026-08-10 16:4x · 2번 지시).
 *
 *   *「여는 날을 잊지 않게 하십시오 — **PG 를 켤 때 noindex 도 같이 풀리게 코드에 묶으십시오.**
 *     ⛔ 손으로 기억하지 않습니다」*
 *
 * ## 왜 — 스위치가 **셋으로 흩어져 있었다**
 *
 *   ```
 *   다음칸.살수있나        false   사는 단추가 서나
 *   파는지면검색.연다       true    114장을 검색에 여나   ← 🔴 어긋나 있었다
 *   (PG 자체)             없음    스위치가 아예 없었다
 *   ```
 *   ⛔ **못 사는데 검색에는 열려 있었다.** 와도 못 사는 지면 114장이 심사에 나쁘고,
 *     헛걸음한 손님은 안 돌아온다(2번이 셋을 들어 닫기로 정했다).
 *
 * ## ⭐ 그래서 **이 한 줄이 셋을 다 움직인다**
 *
 *   ```
 *   PG붙었나 = false → 사는 단추 없음 · 114장 noindex · 사이트맵에서 빠짐
 *   PG붙었나 = true  → 셋이 한꺼번에 열린다
 *   ```
 *   ⚠ 되돌리는 것도 이 한 줄이다. 개봉날 차례표 ③(되돌리는 것)이 이 줄을 가리킨다.
 *   ⛔ 이 줄 말고 다른 데서 `살수있나`·`연다` 를 손으로 켜지 않는다. 켜면 또 갈린다.
 *
 * 🖐 켜기 전에 남의 손 둘이 먼저다 — **PG(사장님)** · **KLifeMap 요금표 regionSet(1번)**.
 *   ⛔ 둘 없이 켜면 손님이 「잘못된 주문」 화면을 본다.
 */
export const PG붙었나 = false;

/** 지역 한 벌 값. 2번·1번·3번이 모여 정했다(2026-08-08 05:4x) */
export const 지역한벌값 = 9900;

/** 화면에 쓰는 꼴 — 「9,900원」 */
export const 값글자 = `${지역한벌값.toLocaleString()}원`;

/**
 * 한 벌에 무엇이 들어 있나. ⛔ **없는 것을 적지 않는다** —
 * 여기 적은 것은 지금 지면이 실제로 보여 주는 것뿐이다.
 */
export const 한벌에드는것 = [
  '그 지역 고등학교를 한 표에 — 갈래 · 졸업생 · 진학 · 그 밖',
  '퍼짐 — 가장 낮은 곳부터 가장 높은 곳까지, 그 안에서 어디쯤인지',
  '전국 갈래별 「그 밖」 가운데값 — 갈래가 다르면 왜 숫자가 다른지',
  '진학률이 없는 학교도 자리에 두고 왜 없는지',
  '이 한 장에 없는 것 — 성적 · 합격 실적 · 어느 대학인지',
] as const;

/**
 * 다음 칸. **아직 살 수 없다**는 것을 먼저 말하고, 그다음에 받을 자리를 준다.
 * ⛔ 「지금 바로」·「놓치지 마세요」를 쓰지 않는다.
 */
export const 다음칸 = {
  /* 🔴 PG 하나에 묶었다(2026-08-10 16:4x). ⛔ 여기서 손으로 켜지 않는다 */
  살수있나: PG붙었나,
  머리: '곧 엽니다',
  말: '아직 사실 수 없습니다. 열리는 날 알려 드릴까요?',
  받는곳: 'sibcheongan@gmail.com',
  단추: '열리면 알려 주세요',
} as const;

/**
 * 🔴 파는 지면 맨 위에 다는 한 줄 (2026-08-08 14:2x · 2번 실측으로 잡혔다).
 *
 *   *「값이 정해지기 전」이 파는 곳에서도 **6/10 장에만** 있습니다.
 *     나머지 4장은 이미 없습니다 — **지면끼리 말이 다릅니다.** 한 곳에서 오게 하십시오」*
 *
 *   ⛔ 지면마다 손으로 적어 두면 **한쪽만 고쳐져 조용히 갈라진다.**
 *     값(9,900)을 여기 모아 둔 것과 같은 까닭이다.
 *
 * ## 🔴 그리고 **오늘 이미 바뀌었어야 할 말이었다** (2026-08-08 14:4x · 2번 라이브 실측)
 *
 *   *「⬜ **값이 정해지기 전이라** 검색에 열지 않았습니다. … 이 한 벌은 **9,900원**입니다」*
 *
 *   ⛔ 한 화면에서 **값이 없다고 하고 값을 말한다.** 파는 곳 6/10 장이 그랬다.
 *     둘 중 하나는 거짓말이다.
 *
 *   ⚠ 값은 오늘 아침에 정해졌다. **안 여는 까닭이 그때 바뀐 것이다** —
 *     「값이 없어서」가 아니라 **「파는 것이라서」**다.
 *     나는 이 자리에 *「여는 날 통째로 바뀐다」*고 적어 두었는데, 그날은 여는 날이 아니라
 *     **값이 정해진 날**이었다. 까닭이 바뀌는 날을 하루 늦게 잡아 둔 것이다.
 *
 * ⚠ 이 말은 **파는 지면을 검색에 여는 날 또 바뀐다.** 그때 이 한 줄만 고치면 114장이 같이 바뀐다.
 */
/* ⚠ 파는 지면 알림은 **스위치 아래**로 옮겼다 — const 는 끌어올려지지 않는다 */

/** 무료 지면에 다는 한 줄. ⛔ 여기에 값을 적지 않는다 — 무료다 */
export const 무료지면알림 = '이 지면은 무료입니다.';

/**
 * 🔴 **파는 지면 114장을 검색에 언제 여나** — 스위치를 **한 곳**에 둔다.
 *
 * ## 지금 (2026-08-09)
 *
 *   ```
 *   파는 지면 114장   noindex · 사이트맵에 없음   → 검색에서 안 보인다
 *   무료 지면 144장   열려 있음
 *   ```
 *
 * ## ⚠ 왜 날짜만으로 열지 않나
 *
 *   2번 03:2x — *「전부 닫으면 아무도 모르고, 전부 열면 살 이유가 없습니다」*
 *   **미리보기를 어디까지 열지가 아직 안 정해졌다.** 날짜만 걸어 두면 그날
 *   지면이 **통째로** 열린다 — 지금 파는 지면은 26곳 표를 **다 보여 준다**(실측).
 *   그러면 여는 순간 팔 것이 없어진다.
 *
 *   ⛔ 그래서 `연다` 는 **손으로 켜는 스위치**다. 날짜가 저절로 켜지 않는다.
 *   ⛔ 내가 혼자 켜지 않는다. 2번이 미리보기 범위를 정해 주면 그때 켠다.
 *
 * ## ⚠ 켤 때 같이 해야 하는 것
 *
 *   ```
 *   ① 미리보기 범위를 지면에 넣는다 (몇 곳까지 보이나)
 *   ② JSON-LD 에 isAccessibleForFree: false + 가린 부분의 cssSelector
 *      ⛔ ② 를 빼고 열면 구글이 「낚시 지면」으로 본다
 *   ```
 *   ⚠ ①이 없으면 ②를 쓸 수 없다 — 가린 부분이 없는데 가렸다고 적는 셈이다.
 *
 * ⭐ 이 자리를 만든 까닭 — 스위치가 `[slug].astro` 와 `sitemap.xml.ts` **두 곳**에 있으면
 *   한쪽만 켜져 「사이트맵엔 있는데 지면은 noindex」가 된다. `/after`·`/region` 때
 *   두 번 그랬다. 두 파일이 **여기 하나**를 부른다.
 */
export const 파는지면검색 = {
  /**
   * ✅ **2026-08-09 09:1x 에 켰다** (2번 지시 08:4x).
   *
   *   *「지금 파는 지면 114장은 noindex 라 구글이 그런 것이 있는 줄도 모릅니다.
   *     물건을 창고에 넣고 문을 잠근 것입니다」*
   *
   *   ⛔ 켜기 전에 미리보기·JSON-LD·맨 위 알림이 **먼저** 들어가 있어야 했고,
   *     09:0x 에 다 넣고 켜 봤다가 껐다. 이번엔 그대로 둔다.
   * ⚠ 되돌리려면 이 한 줄만 false 로 놓으면 넷이 같이 돌아간다 —
   *   검색 열림 · 숫자 다섯 곳 · JSON-LD · 맨 위 알림.
   */
  /**
   * 🔴 **2026-08-10 16:4x 에 도로 닫았다 — 그리고 PG 에 묶었다** (2번이 정함).
   *   ⛔ 못 사는데 검색에는 열려 있었다. 2번이 든 까닭 셋 —
   *     ① 애드센스 심사 중이라 「값만 있고 못 사는 지면 114장」이 나쁘다
   *     ② PG 전에는 와도 못 산다. 헛걸음한 손님은 안 돌아온다
   *     ③ 114장은 4,971장의 2.3% — 열어도 유입이 크지 않다
   *   ⭐ 이제 PG 를 켜면 **저절로 같이 열린다.** 여는 날을 손으로 기억하지 않는다
   */
  연다: PG붙었나,
  /** 8/9 에 한 번 열었다가 8/10 에 닫았다. 다시 여는 날은 **PG 가 붙는 날**이다 */
  여는날: 'PG 붙는 날',
  막는것: 'PG 가 없어 못 산다 — 와도 헛걸음이다',
} as const;

/**
 * 🔴 **미리보기 — 무엇을 보이고 무엇을 가리나** (2026-08-09 07:1x · 2번이 정함).
 *
 *   *「파는 것은 **「우리 아이 학교가 어떤가」** — 개별 학교의 숫자.
 *     공짜인 것은 **「이 지역이 대충 어떤가」** — 가운데값·퍼짐.
 *     요약을 팔면 검색에서 안 걸리고, 개별을 공짜로 주면 살 이유가 없다」*
 *
 *   ```
 *   ① 학교 이름       **전부 보인다**            ⭐ 검색에 걸리는 글자다
 *   ② 진학률 숫자     **가나다순 앞 다섯 곳만**   ⛔ 나머지는 「가려짐」
 *   ③ 갈래별 가운데값 · 퍼짐   **보인다**         ⭐ 「이 지역이 대충 어떤가」는 공짜
 *   ④ 출처 · 진학률 없는 곳    **보인다**         ⭐ 못 대는 것을 밝히는 것은 늘 공짜
 *   ```
 *
 * ## ⛔ 다섯 곳은 **가나다순**이다. 고르지 않는다
 *
 *   2번 — *「「진학률 높은 다섯 곳」으로 하면 우리가 골라 보여 주는 것이 된다.
 *   「큰 학교 다섯 곳」도 마찬가지다. **가나다순 앞에서 다섯** — 규칙이 한 줄이고
 *   우리가 손댈 자리가 없다」*
 *   ⚠ 그래서 지면에 **「가나다순 앞 다섯 곳입니다」**라고 적는다. 왜 이 다섯인지 손님이 알아야 한다.
 *
 * ## 🔴 숫자가 **표 말고 다른 데로도 샌다**
 *
 *   ```
 *   퍼짐 띠의 점    title="○○고등학교 58.2%"   ⛔ 이름과 숫자가 짝지어 들어 있다
 *   「낮은 것이…」   가장 낮은 곳의 이름과 「그 밖」 값을 글로 적는다
 *   ```
 *   ⛔ 표만 가리고 이 둘을 두면 **가린 것이 아니다.** 점은 남기되(퍼짐은 공짜)
 *     **이름표를 뗀다.** 글 쪽은 그 학교가 보이는 다섯에 들 때만 이름과 값을 적는다.
 */
const 파는지면알림_닫힘 =
  '파는 지면이라 검색에 열지 않았습니다. 무료로 여는 곳은 검색에서 바로 열립니다.';
/**
 * 🔴 **여는 날 이 말이 거짓이 된다** — 그래서 스위치를 따라가게 한다 (2026-08-09 08:4x).
 *
 *   위 주석에 *「이 말은 파는 지면을 검색에 여는 날 또 바뀐다」*고 **적어만 두었다.**
 *   적어 두는 것으로는 안 바뀐다. 8/15 에 스위치를 켜면 지면이 검색에 서면서
 *   맨 위에 **「검색에 열지 않았습니다」**라고 말하게 된다. 한 화면에서 말이 갈린다.
 *   ⛔ 8/8 에 「값이 정해지기 전」으로 똑같이 당했다. **두 번째는 안 만든다.**
 */
export const 파는지면알림 = 파는지면검색.연다
  ? '파는 지면입니다. 숫자는 일부만 열려 있고, 나머지는 한 벌을 받으시면 함께 열립니다.'
  : 파는지면알림_닫힘;

export const 미리보기 = {
  /** 숫자를 보여 줄 곳수. ⛔ 「높은 순」이 아니라 **가나다순 앞** */
  숫자보이는곳: 5,
  고르는법: '가나다순 앞',
  가린말: '가려짐',
  /** 지면에 적는 한 줄. ⚠ 왜 이 다섯인지 손님이 알아야 한다 */
  /**
   * ⚠ **정확히 적는다.** 앞서 「가나다순 앞 다섯 곳」이라고만 적었더니
   *   앞 다섯 줄 중 둘이 진학률 없는 학교라 **손님은 셋을 봤다.**
   *   ⛔ 약속과 주는 것이 다르면 그건 흠이 아니라 거짓말이다(2번 12:4x).
   */
  설명: '숫자는 진학률이 있는 학교 중 가나다순 앞 다섯 곳만 보입니다. 나머지는 한 벌을 받으시면 함께 열립니다.',
  /** JSON-LD 가 가리킬 자리. ⚠ 지면의 class 이름과 **한 글자도 달라선 안 된다** */
  가린곳표시: '.paywall',
} as const;

/** 메일 한 통으로 끝나게 제목·본문을 미리 채운다. ⚠ 개인정보를 주소에 싣지 않는다 */
/* ────────────────────────────────────────────────────────────────
 * 🔴 **사는 길** — 2번 지시(2026-08-10 08:35) *「값을 눌러도 살 수가 없습니다」*
 * ──────────────────────────────────────────────────────────────── */

/**
 * ## 어디로 보내나 — **KLifeMap 결제로 넘긴다.** 이렇게 정했다
 *
 *   ```
 *   ① 백년지도가 직접 받나   ⛔ 우리 저장소에 결제 코드 **0곳**이다.
 *                             그리고 2번이 「결제 화면을 새로 만들지 마라」 하셨다
 *   ② KLifeMap 으로 넘기나   ⭐ 이미 다 있다 — 요금표·쿠폰·계좌이체·환불·관리자 입금확인
 *                             (klifemap/server.js:3008 `/api/billing/checkout`)
 *   ```
 *   ⭐ 사장님 말씀 *「KLifeMap 고객과 백년지도 고객은 같다」* 와도 맞는다.
 *
 * ## 🔴 그런데 **지금 켜면 손님이 오류 화면을 본다.** 재서 알았다
 *
 *   ```
 *   klifemap /api/billing/prices 의 상품 **14개** — 지역 한 벌이 **없다**
 *      saju · sajuAiReport · gunghap · taekil · sungmyung · careerCoach ·
 *      parentCoaching · childCareerCoaching · coupleLoverCoaching ·
 *      separationCoaching · personalityCoaching · lifeDesignCoaching ·
 *      intelligenceOne(9,900) · intelligenceAll
 *   payMethods.bankTransfer = **false**  ← 계좌이체도 아직 꺼져 있다
 *   ```
 *   ⛔ 없는 `service` 로 보내면 checkout 화면이 **「잘못된 주문」 칸**을 띄운다.
 *     막다른 길을 **오류 화면**으로 바꾸는 것은 나아지는 게 아니다.
 *
 * ⭐ 그래서 **길은 코드에 다 내 두고, 켜는 것은 `살수있나` 한 줄**로 둔다.
 *   🖐 켜기 전에 **KLifeMap 요금표에 한 줄**이 있어야 한다(1번 손) — `사는상품` 참고.
 */
export const 사는상품 = 'regionSet';

/** KLifeMap 결제 화면 — ⚠ 주소를 지면에 손으로 적지 않는다. 여기 한 곳이다 */
export const 사는곳뿌리 = 'https://klifemap.ai/checkout.html';

/**
 * 사는 데로 가는 주소.
 * @param 지역이름 어느 지역 한 벌인가 — 딱지에 실어 보낸다
 *
 * ⚠ 딱지를 **셋 다** 단다 — `from` · `at` · `area`.
 *   `?from=` 은 우리 서버가 물음표 뒤를 안 적어 우리 쪽에선 못 세지만,
 *   **받는 쪽(KLifeMap)은 센다.** 1번이 그 자리를 이미 만들어 두었다.
 */
export function 사는주소(지역이름?: string): string {
  const 딱지 = new URLSearchParams({ type: 'single', service: 사는상품, from: '100y', at: 'price' });
  if (지역이름) 딱지.set('area', 지역이름);
  return `${사는곳뿌리}?${딱지.toString()}`;
}

export function 알림메일주소(지역이름: string): string {
  const 제목 = `[백년지도] ${지역이름} 한 벌이 열리면 알려 주세요`;
  const 본문 = `${지역이름} 지역 한 벌이 열리면 이 메일로 알려 주세요.`;
  return `mailto:${다음칸.받는곳}?subject=${encodeURIComponent(제목)}&body=${encodeURIComponent(본문)}`;
}
