/**
 * 도는자리.mjs — **지금 도는 자리가 어디인가. 이 회사에 한 곳만 있는 답이다.**
 * ═══════════════════════════════════════════════════════════════════════════
 * 🔴 [2026-09-19 · 사장님] **「언제 세션을 정리했는데 아직도 헤매나?」**
 *
 * [무엇이 문제였나 — 오늘 하루에 네 번 걸렸다]
 *   자리를 1·2·5 셋으로 줄인 것은 2026-09-18 이다. 그런데 저장소 곳곳이 아직
 *   접힌 자리(3·4·6·7번)를 «살아 있는 자리»로 기다리고 있었다.
 *     · klifemap 감수 고리가 6번 차례에서 멈춰 **9/16 이후 배포가 통째로 막혔다**
 *       (라이브 daeun-chart.js 44,958B · HEAD 87,568B — 절반짜리 옛 코드가 며칠 나갔다)
 *     · 유닛 좌석 자가 허락된 자리를 6·7번으로 들고 있어 매시 빨간불을 켰다
 *     · 도장 대장의 마지막 통과가 3번 이름이라 다음 사람이 「3번이 도나」로 읽는다
 *     · 방문자 방송·지면 점검이 백년지도를 「3번」, 서울마켓츠를 「6번」이라 부른다
 *
 * [왜 되풀이됐나 — 고친 방식이 틀렸다]
 *   걸릴 때마다 «그 한 곳»을 고쳤다. 그래서 다음 자가 또 걸리고, 또 고치고, 또 걸린다.
 *   답이 다섯 곳에 흩어져 있으면 자리를 한 번 옮길 때마다 다섯 번 고쳐야 하고,
 *   **한 곳이라도 빠지면 그것이 조용히 일을 막는다.** 오늘 막힌 것이 바로 그 한 곳이었다.
 *
 * ⇒ 답을 «한 곳»에 둔다. 자리가 바뀌면 이 파일만 고친다.
 *   ⛔ 다른 파일에 자리 목록을 다시 적지 않는다. check-자리목록-한곳.mjs 가 그것을 잡는다.
 *
 * ⚠ 이 파일은 «누가 도는가»만 안다. «무엇을 맡았나»는 아래 맡은일 에 있고,
 *   그것도 원문이 있는 것만 적는다 — 못 쟀으면 못 쟀다고 적지 짐작해서 채우지 않는다.
 */

/** 지금 도는 자리. 사장님 — 「내일부터 1, 2, 5번 세션만 보고해라」(2026-09-18) */
export const 도는자리 = ['1번', '2번', '5번'];

/**
 * 접힌 자리. ⛔ «없는 것»으로 지우지 않는다 —
 *   ① 자격증명이 아직 남아 있어 누가 그 창을 열면 그 계정으로 돈다(사장님 몫이 깎인다)
 *   ② 옛 기록(도장 대장·메모·커밋)에 이름이 남아 있어, 읽는 자가 「이 자리가 도나」를 물어야 한다
 */
export const 접힌자리 = ['3번', '4번', '6번', '7번'];

/** 자리마다의 설정 뿌리(그 창이 어느 계정으로 도는지는 여기 .claude.json 에 있다) */
export const 설정뿌리 = {
  '1번': '.claude-u1',
  '2번': '.claude-u2',
  '5번': '.claude-u5',
  /* 접힌 자리도 남긴다 — 위 ①의 까닭. check-seat-split 이 여기를 살핀다 */
  '3번': '.claude-u3',
  '4번': '.claude-u4',
  '6번': '.claude-u6',
};

/**
 * 사이트마다 «지금» 누가 맡나.
 * ⚠ 원문이 있는 것만 적는다. docs/업무분장-2026-09-12.md 는 여섯 자리 시절 것이라
 *   그대로 믿으면 안 된다 — 그 뒤 사장님이 자리를 셋으로 줄이셨다.
 *
 * 근거 (5번 세션 입구 카드 · 사장님이 정하신 지금 분장)
 *   「5번  SeoulMarkets 개발 · 감수 · 유료 사이트 자물쇠」
 *   「2번  SeoulMarkets 개발 분담」 · KLifeMap 손님길 점검
 *   「1번  «모든 사이트» 콘텐트 — 네 곳 × 6편 = 하루 24편」
 */
export const 맡은일 = {
  콘텐트: '1번',                    /* 네 사이트 전부 */
  사이트: {
    'SeoulMarkets': { 개발: '5번', 분담: '2번' },
    'KLifeMap': { 개발: '5번', 손님길: '2번' },
    '백년지도': { 개발: null },      /* ⬜ 못 쟀다 — 사장님이 따로 정해 주신 원문을 못 찾았다 */
    'K Culture Wire': { 개발: null },/* ⬜ 못 쟀다 — 콘텐트만 돈다(명맥 유지) */
  },
};

/** 그 자리가 지금 도나 */
export function 도나(자리) {
  return 도는자리.includes(String(자리 ?? '').trim());
}

/** 목록에서 도는 자리만 남긴다 — «차례»는 그대로 두고 없는 자리만 뺀다.
 *  사장님 규칙 「중간에 빠진 세션이 있으면 그 다음 세션이 이어받는다」(2026-09-13) 가 이것이다. */
export function 살아있는것만(목록) {
  return (Array.isArray(목록) ? 목록 : []).filter((자리) => 도나(자리));
}

/** 자리 이름을 고르게 만든다 — '5', 5, '5번' 을 다 '5번' 으로 */
export function 자리이름(값) {
  const 글 = String(값 ?? '').trim();
  if (!글) return null;
  const m = 글.match(/^(\d+)\s*번?$/);
  return m ? m[1] + '번' : 글;
}
