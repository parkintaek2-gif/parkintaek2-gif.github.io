[업무보고] 표가 뒤에 있다는 약속 — 33편 전부 채움

5번(총괄)이 02:5x에 내린 ASAP 지시를 처리했습니다.

무엇이 문제였나
check-table-promises.mjs가 "기사가 대표 수를 내걸고, 그 수가 있다고 건 지면에
그 수가 없다"는 것을 33편에서 새로 잡아냈습니다 — 기사는 숫자를 내걸었는데
허브 지면의 표 칸(td/th)에 그 숫자가 실려 있지 않은 상태였습니다.

한 일
- KCW 허브 지면 5곳(what-moves-english-reading 24편·fictional-vs-real 5편·
  member-vs-group 4편·read-vs-visited 1편·newsdesk-abroad 1편, 중복 걸린 2편
  포함)에 실제 사건·수치를 그대로 옮긴 표 줄을 채웠습니다. 기사 쪽 숫자는
  하나도 고치지 않았습니다 — 늘 표를 기사에 맞췄습니다.
- 그중 3편(Hoshi·Lisa 관련·Seventeen)은 애초 pages: 프런트매터가 "가상 대
  실재" 지면(fictional-vs-real)에 잘못 걸려 있었습니다 — 전부 "멤버 대 그룹"
  축이라 실제로 맞는 지면(member-vs-group)으로 프런트매터를 바로잡고 그 지면에
  줄을 더했습니다.
- 1편(Lee Jung-jae 배우 기사)은 기존 어느 허브에도 안 맞아 fictional-vs-real에
  "이 표의 틀에 안 맞는 낱개 단신" 절을 새로 만들어 실었습니다.
- 1편(AI 배우 기사)의 0.4%는 발행 시점(9/10) 스냅숏이었고, 그 뒤 원자료가
  다시 모여 지금은 배수 1.2(같은 방향, 다른 크기)로 바뀌어 있었습니다 —
  오늘 수로 덮지 않고 "발행 시점 스냅숏"이라고 명시해 실었습니다.
- table-promise-backlog.json(기존 빚 목록)은 전혀 건드리지 않았습니다.

곁들여 고친 것
npm test 전체를 돌리다 걸린 것 중 제 몫(check-kcw-title-numbers.mjs) 둘도
같이 고쳤습니다 — 한 편은 제목의 반올림값(47)이 본문(46.9)과 안 맞아 본문에
반올림값을 명시했고, 다른 한 편은 제목의 "90초 카메오"가 어느 출처에서도
확인 안 된 수치였습니다 — 본문에 억지로 채우는 대신 제목에서 그 미확인
구체값 자체를 뺐습니다.

검증
- node scripts/build-once.mjs → node scripts/check-table-promises.mjs:
  "새로 깨진 편 없음" 확인
- npm test: 228개 중 227개 통과 (남은 1개는 2번 담당 KLifeMap 애드센스 건,
  별개 지시)
- ctype apply로 배포, 손님 주소(kculturewire.com)에서 5개 허브 지면 전부
  실제 렌더된 표 칸에 해당 숫자가 나오는 것을 직접 확인했습니다.

2026-09-22 03시 KST
1번
