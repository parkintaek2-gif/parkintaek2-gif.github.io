# 사장님 접속 자원 — 사이트별 어드민

> 사장님 지시(2026-08-22): 「각 사이트 어드민 페이지를 내가 들어갈 수 있게 만들고, 자원목록에 넣어라. 내 아이디·비번 다 똑같이」

⛔ **비밀번호는 이 문서(git 저장소)에 적지 않는다.** 저장소가 공유·백업되면 그대로 새어 나간다.
   비밀번호는 2번이 사장님께 대화로만 전달했다. 여기는 **어디로 가면 되는지**만 적는다.

| 사이트 | 어드민 주소 | 아이디 | 비밀번호 | 상태 |
|---|---|---|---|---|
| seoulmarkets.com | `/admin` (예: `/admin/traffic`) | parkintaek2@gmail.com | 대화로 전달(2026-08-22) | ✅ 재발급 완료 |
| 100yearmap.com | `/admin` | parkintaek2@gmail.com | 위와 **동일** | ✅ 재발급 완료 (seoulmarkets와 같은 코드/서버) |
| kculturewire.com (WikiTip) | `/admin` | parkintaek2@gmail.com | 위와 **동일** | ✅ 재발급 완료 |
| klifemap.ai | 방식이 다름 — 고정 주소 아님, 관리자 키를 해시한 22자 숨긴 경로(사장님이 기존에 쓰시던 그 주소 그대로) | 구글/네이버/카카오 로그인 중 최고관리자 지정 계정, 또는 관리자 키 | 대화로 전달(기존과 동일) | ✅ 확인 완료(2026-08-23) — 다른 세 사이트처럼 "같은 아이디·비번"은 아니고 방식 자체가 다름 |

## 지금 로그인하는 법

**seoulmarkets·100yearmap·kculturewire** — 브라우저에서 위 주소를 열면 **HTTP 기본인증
창**(브라우저가 띄우는 아이디/비번 입력창)이 뜬다. 아이디는 `parkintaek2@gmail.com`,
비밀번호는 대화에서 드린 것을 넣는다.

**klifemap.ai** — 주소창이 아니라 **사장님이 기존에 쓰시던 숨긴 관리자 주소**로 바로
들어간다(고정 주소 아님, 새로 찾을 필요 없음). 들어가면 화면 안에 "점성학" 탭 등
여러 탭이 있다.

## 비밀번호를 다시 바꾸려면

`node -e "const{scryptSync,randomBytes}=require('crypto');const s=randomBytes(16).toString('hex');console.log(s+':'+scryptSync('새비밀번호',s,64).toString('hex'))"`

나온 값을 `server.mjs`의 `ADMIN_HASH`에 넣는다(dataeconomics·wikitip 둘 다 같은 값으로).
