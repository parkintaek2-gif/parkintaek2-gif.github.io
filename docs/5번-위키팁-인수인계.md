# 5번 세션 인수인계 (WikiTip) — **이 문서 하나로 3일을 일한다**

> 작성 **2026-08-05 11:5x KST** · 2번(SeoulMarkets) → **5번 세션**
>
> 사장님이 5번에게 하신 정의: **「네가 할 일은 영어뉴스 + 데이터가공이다.
> K팝 등에 관심이 많은 해외 대상이다」**
>
> 사장님 지시(2026-08-05):
> **「위키팁을 새 세션에서 하자. 내 손이 안 가도록 네가 인수인계를 잘 해야 함.
>  초기 3일 정도는 네가 작업지시를 한다고 해야 함. 그 이후는 내가 함. api 키 등도 다 줘」**
>
> 🔴 **3일간(8/5~8/7) 작업지시는 2번이 한다.** 막히면 사장님이 아니라 **나에게** 묻는다.
>   묻는 곳은 `docs/세션간-메모.md` 꼬리. 8/8부터는 사장님이 직접 지시하신다.

---

## Ⅰ. 무엇을 만드는가

### 1-1. 한 줄

**K컬처·e스포츠 데이터를 영문으로 가공해 발행한다. 독자는 동남아 영어권이다.**

### 1-2. 🔴 사이트를 헷갈리지 않는다 — 이미 한 번 사고가 났다

```
SeoulMarkets  금융·경제    증시·외환·채권·커머디티·거시     seoulmarkets.com
WikiTip       K컬처·e스포츠 K팝·드라마·영화·웹툰·e스포츠    wiki-tip.com   ← 당신
백년지도       교육·진로     학과·취업률·중도탈락률          100yearmap.com
```

**e스포츠 지면을 금융 매체(SeoulMarkets)에 얹고 그 이름으로 Riot 에 신청한 적이 있다.**
판정이 애매하면 **「누가 읽는가」**로 가른다. 동남아 K컬처 독자면 당신 것이다.

⚠ 수집기 이름과 아카이브 경로에 **매체를 드러낸다.** `collect-x.mjs` 처럼 짓지 않는다.

---

## Ⅱ. 지금 있는 것 — **전부 2026-08-05 실측**

### 2-1. 지면 (이미 있다. 처음부터 만들지 않는다)

```
src/pages/wikitip/index.astro     첫 화면
src/pages/wikitip/about.astro     소개
src/pages/wikitip/esports.astro   랭크 사다리
src/pages/esports.astro           ⚠ Riot 심사용. **지우지 않는다**
src/layouts/WikiTip.astro         공통 레이아웃
```

### 2-2. 수집기와 자료

| 수집기 | 무엇 | 지금 보유 |
|---|---|---|
| `scripts/collect-riot-ladder.mjs` | LoL 랭크 사다리 6지역 | `archive/raw/riot-ladder/` 3일치 (08-02~04) |
| `scripts/collect-netflix-top10.mjs` | 넷플릭스 주간 순위 | `archive/raw/netflix-top10/` 한국 작품·출연진 |
| `scripts/collect-star-pageviews.mjs` | 배우 페이지뷰 | `archive/raw/star-pageviews/actors-20260803.json` |
| `scripts/probe-riot.mjs` | Riot API 찔러보기 | — |

빌드가 쓰는 자료: `src/data/riot-ladder.json` · `src/data/wikitip-charts.json`

### 2-3. 🔴 도메인이 아직 안 붙었다

```
wiki-tip.com  →  302  →  errdoc.gabia.io/403.html    ← **남의 것**
```

**네임서버가 아직 Cloudflare 로 안 넘어왔습니다.** 사장님 손이 필요한 항목이고
`docs/사장님-손.md` 3번에 링크와 함께 올라가 있습니다. **당신이 재촉하지 않는다** —
내가 2시간마다 확인한다. 넘어오면 알려 준다.

그동안 지면은 **로컬에서 Host 헤더로** 확인한다.

```bash
npm run build
PORT=4321 node server.mjs &
curl -H "Host: wiki-tip.com" http://127.0.0.1:4321/ -s -o /dev/null -w "%{http_code}\n"
```

⚠ **`fetch` 로 Host 별 동작을 시험하지 마라.** undici 가 `Host` 헤더를 버려서
**버그가 있어도 200 이 나온다.** 오늘 내가 그걸로 두 번 헛돌았다.
`curl -H` 나 `node:http` 의 `request()` 를 쓴다.

---

## Ⅲ. API 키 — **값은 `.env` 에 있다. 문서에 옮겨 적지 않는다**

🔴 **이 저장소는 공개다.** 키 값을 커밋하면 전 세계가 읽는다.
당신은 **같은 PC** 에서 돌므로 같은 `.env` 를 읽으면 된다.

```
C:\Users\USER\Documents\GitHub\dataeconomics\.env
```

### 3-1. 당신이 쓸 키

| 이름 | 무엇 | 상태 |
|---|---|---|
| `RIOT_API_KEY` | Riot 랭크 사다리 | ⚠ **24시간마다 만료**. 아래 3-2 |
| `DATAGO_KEY` | 공공데이터포털 (KOCCA·문화 통계 등) | 승인됨 · ⚠ **이미 URL 인코딩된 값**. `encodeURIComponent` 를 한 번 더 걸면 403 |
| `RAPIDAPI_PROXY_SECRET` | — | 용도 확인 필요 |

읽는 법은 기존 수집기를 그대로 베낀다 (`scripts/collect-riot-ladder.mjs` 참고).
**키를 `console.log` 에 찍지 않는다.**

### 3-2. 🔴 Riot 키는 매일 죽는다

Personal Key 는 **24시간마다 만료**된다. 만료되면 수집이 조용히 실패한다.

```
① developer.riotgames.com 로그인 → REGENERATE
② .env 의 RIOT_API_KEY 교체
③ npm run collect:riot 로 확인
```

**사장님 손이 필요한 유일한 반복 작업이다.** 세션 시작할 때마다 마지막 수집 시각을 본다 —
`scripts/session-brief.mjs` 가 자동으로 알려 준다(26시간 넘으면 경고).

---

## Ⅳ. 🔴 데이터를 받기 **전에** 라이선스를 본다

**오늘 내가 이걸 어겨서 152,396건을 받고 버렸다.** 공공누리 2유형(상업적 이용금지)이었다.

```
✕ API 를 부르고 → 받고 → 그 다음에 이용허락범위를 봤다
✅ 데이터셋 상세화면에서 이용허락범위를 보고 → 대장에 줄을 만들고 → 부른다
```

**부르는 데 3분, 확인하는 데 3분이다. 순서만 바꾸면 된다.**

판정표는 `docs/데이터-라이선스-대장.md` 다. **새 소스를 쓰기 전에 거기 줄을 만든다.**

```
🟢 판매 가능   상업 이용·변형 허용 (「제한 없음」 또는 공공누리 1유형)
🟡 발행만     기사에는 쓰되 표로 안 판다
🔴 못 쓴다     2·4유형 등. **뚫려도 안 쓴다**
```

⚠ 3번(백년지도)이 고용24 492건을 반나절 걸려 뚫고 나서 4유형인 걸 알고 통째로 버렸다.
   **경로를 바꿔도 라이선스는 안 바뀐다.**

### 4-1. 막혔을 때 — 사장님 지시(2026-08-05)

> **「1. 메일 안 보내고 해결해. 안 되면 2. 메일 보내서 해결해」**
> **「이런 게 생기면 찔러봐」**

「못 쓴다」에서 멈추지 않는다.

```
① **같은 사실을 주는 다른 출처**를 먼저 찾는다
   오늘 실례 — 예탁결제원 2유형 → **DART 가 같은 것을 제한 없음으로, 더 자세히** 줬다
② 없으면 데이터셋 상세화면에서 **문의처**를 찾아 보낸다
   초안 꼴은 `docs/대외/예탁결제원-정보이용계약-문의.md` 참고
```

**막힌 것은 그 기관의 「표현」이지 「사실」이 아니다.** 사실에는 저작권이 없다.

---

## Ⅴ. 데이터 소스 — 확인된 것

| 소스 | 무엇 | 상태 |
|---|---|---|
| **Riot** | LoL 랭크 사다리 | 🟡 **Production 승인 전 광고 금지** · 수집 중 |
| KOBIS | 영화 | robots 허용 확인됨 |
| KOPIS | 공연 | OpenAPI |
| KOCCA | 콘텐츠산업 통계 | — |
| KOMACON | 웹툰 | — |
| 써클차트 | 음악 | — |
| 문화빅데이터플랫폼 | 종합 | — |
| 멜론 | **`Allow: /global-k-chart` 만** | 그 축만 본다 |
| Netflix Top10 | 주간 순위 | ⬜ **라이선스 미확인** — 쓰기 전에 확인 |

⚠ **위 목록은 「소스가 있다」까지다.** 라이선스는 KOBIS·멜론 robots 말고는 **아무것도 확인 안 됐다.**
   내가 확인한 것처럼 쓰지 마라. Ⅳ 순서대로 하나씩 확인하고 대장에 적는다.

---

## Ⅵ. 🔴 지켜야 할 것

### 6-1. 광고 — Riot 심사 중이다

```
⛔ /esports · /wikitip/esports 두 지면에 **광고 코드 0건**을 유지한다
⛔ Riot Production 승인 전에는 어떤 수익화도 붙이지 않는다
```

### 6-2. 우리는 아직 언론사가 아니다

인터넷신문 등록이 안 돼 있다.

```
⛔ 「우리는 언론사다」를 자료 요청·소개·고지 어디에도 쓰지 않는다
⛔ 소속사 사진처럼 **언론 관행에 기대는 이미지 사용**을 하지 않는다
✅ 대신 **보도자료를 배포하는 쪽**에 선다
```

### 6-3. 아카이빙은 하루도 빠뜨리지 않는다

**소급이 안 되는 유일한 항목이다.**

```
Riot 랭크 사다리   매일 22:00   ← 그날 안 받으면 영영 없다
```

⚠ **당분간 이 예약은 내가 들고 있는다.** 당신이 자리를 잡으면 넘긴다 —
   인수인계 도중에 예약이 공중에 뜨면 그날치가 날아간다. **8/8에 정식으로 넘긴다.**

### 6-4. 시각은 **한국시간(KST)**

이 PC 는 이미 KST 다. `new Date()` 를 그대로 쓴다.
**9시간을 더하지 않고 `toISOString()` 도 쓰지 않는다** (UTC 라 새벽에 날짜가 어긋난다).

### 6-5. 배포 — **직접 하지 않는다**

```
⛔ ctype apply 를 직접 부르지 않는다
✅ npm run deploy 만 쓴다 (락이 걸려 있다)
```

**Cloudtype 총 1GB 를 klifemap(사장님 매출 서비스)과 나눠 쓴다.**
8/3 에 4분 차이로 겹쳐 롤링 업데이트가 25분 멈췄고,
오늘(8/5) 에는 우리 `web` 이 klifemap 스테이지에 잘못 올라갔다.

**당분간 배포는 내가 한다.** 당신은 커밋·푸시까지만 하고 나에게 알린다.

---

## Ⅶ. 저장소를 나와 나눠 쓴다 — **경계**

같은 저장소(`dataeconomics`)에서 둘이 일한다. 부딪히지 않게 나눈다.

### 7-1. 당신 것 (마음대로 고친다)

```
src/pages/wikitip/**        src/layouts/WikiTip.astro
scripts/collect-riot-*.mjs  scripts/collect-netflix-*.mjs  scripts/collect-star-*.mjs
scripts/collect-wikitip-*.mjs   ← 새로 만들 것은 이 이름으로
src/data/wikitip-*.json     src/data/riot-ladder.json
archive/raw/riot-ladder/    archive/raw/netflix-top10/    archive/raw/star-pageviews/
archive/raw/wikitip/        ← 새 아카이브는 여기로
docs/위키팁-*.md
```

### 7-2. 🔴 공용 — **고치기 전에 메모로 알린다**

```
server.mjs        SITE_PREFIX · 라우팅. 세 사이트가 다 걸린다
package.json      스크립트 추가는 괜찮다. test 는 건드리기 전에 알린다
src/consts.ts     SISTER_SITES 등
src/styles/global.css   SeoulMarkets 전용이지만 공용 파일이다
docs/데이터-라이선스-대장.md   줄 추가는 환영. 남의 줄은 안 고친다
docs/세션간-메모.md   **붙이기만 한다.** 남의 글을 고치지 않는다
```

### 7-3. 내 것 (건드리지 않는다)

```
src/pages/*.astro (wikitip 밖)   content/articles/**   src/pages/100y/**
scripts/collect-{stock,bond,fund,issuance,tenure,...}   src/lib/{issuance,traffic}.mjs
```

### 7-4. 부딪히면

`git pull --rebase` 를 먼저 한다. 그래도 충돌하면 **고치지 말고 메모에 적는다.**
남의 파일을 추측으로 병합하면 조용히 망가진다.

---

## Ⅷ. 🔴 3일치 작업지시 (2번이 지시자)

### Day 1 (8/5 남은 시간) — **지금 있는 것을 세어라**

```
1  이 문서를 읽고 `docs/인계-현재상태.md` 와 `docs/세션간-메모.md` 꼬리를 본다
2  npm test 를 돌려 지금 상태를 확인한다 (통과해야 정상)
3  로컬에서 Host 헤더로 wikitip 지면 3장을 **눈으로 본다** (Ⅱ-3 방법)
   ⚠ 「빌드 통과」는 확인이 아니다. **렌더해서 본다**
4  가진 자료를 **센다** — riot-ladder 3일치에 몇 지역·몇 명이 들어 있나
   netflix-top10·star-pageviews 는 무엇이 몇 건인가
5  결과를 `docs/세션간-메모.md` 에 적고 나에게 알린다
```

**Day 1 의 목표는 만드는 게 아니라 「무엇이 있는지 정확히 아는 것」이다.**
있는 걸 모르고 새로 만드는 것이 제일 나쁘다.

### Day 2 (8/6) — **라이선스부터. 그 다음 소스 하나**

```
1  Ⅴ 표의 소스 중 **하나만** 골라 라이선스를 확인하고 대장에 줄을 만든다
   추천 — **KOBIS(영화)**. robots 가 이미 확인됐고 영문 독자에게 바로 붙는다
2  🟢 면 수집기를 만든다. 이름은 `scripts/collect-wikitip-kobis.mjs`
3  실제로 돌려서 **건수를 확인한다.** 「붙였다」가 아니라 「N건 들어왔다」로 보고한다
4  아카이브는 `archive/raw/wikitip/kobis/` 로
```

⚠ **한 번에 여러 소스를 건드리지 마라.** 하나를 끝까지(라이선스→수집→검증→보고) 한다.

### Day 3 (8/7) — **지면 하나를 제대로**

```
1  Day 2 자료로 지면 한 장을 만든다
2  🔴 **그릇이 내용물을 결정한다** — 사장님 필수 지시다
   렌더해서 눈으로 보고, 모바일(360px)에서 보고, 인쇄해서 본다
3  숫자를 쓸 때 **분모를 같이 적는다.** 「60.1%」가 아니라 「60.1% (취업대상자 1,007명 기준)」
4  발행 전에 숫자를 **원본에서 다시 뽑아 대조**한다
   오늘 내가 기사 하나에서 그렇게 해서 **6개 숫자가 틀린 걸 내기 전에 잡았다**
```

---

## Ⅸ. 일하는 법 — 오늘 내가 값을 치른 것들

전문은 `docs/일하는-법-최종본.md`. **여기 있는 건 오늘 실제로 당한 것만.**

```
· **규칙을 두 벌 두지 않는다.** 판정 규칙을 두 군데 적으면 어느 쪽이 맞는지 아무도 모른다
  오늘 나는 같은 실수를 **네 번** 했다 (합계 고르기·이상점검·파일명 자리·검증 스크립트)
  → 정본 하나를 `export` 해서 **import 해 쓴다**

· **검사가 헛도는지 확인한다.** 일부러 틀리게 해 보고 **실제로 실패하는지** 본다
  오늘 라우팅 시험이 **두 번** 헛돌았다 (포트 재사용 · fetch 의 Host 헤더)

· **도구가 입력을 바꾼다.** 윈도 셸은 한글을 CP949 로, fetch 는 Host 를 버린다
  둘 다 **오류가 안 난다.** 없는 병을 고치거나 있는 병을 못 본다

· **원본을 같이 저장한다.** 파서를 고쳤을 때 다시 안 받으려고
  오늘 원문이 없어 2,921번을 다시 불렀고, 있어서 **0번**으로 고쳤다

· **없는 것을 0 으로 만들지 않는다.** null 로 둔다
  0 으로 하면 순위 맨 아래가 오염되고 평균이 틀어진다

· **건수와 크기를 둘 다 본다.** 오늘 건수만 보고 「전환이 1위」라고 했다가
  주식수로는 1/4 인 걸 알았다. 그대로 썼으면 틀린 기사가 나갔다

· **재 보고 안 된다고 적는 것도 결과다.** 억지로 만들어 내지 않는다
```

---

## Ⅹ. 막히면

```
1  `docs/세션간-메모.md` 꼬리에 적는다 — 무엇을, 어디까지 했고, 무엇이 막혔나
2  8/7까지는 **나(2번)** 가 답한다. 사장님께 바로 올리지 않는다
3  사장님 손이 꼭 필요하면 `docs/사장님-손.md` 에 **링크와 누르실 곳까지** 적는다
   ⚠ 「어디 가서 하세요」는 안 적는다. 사장님 지시다
```

**한 일은 커밋 메시지에 남긴다.** 세션이 죽어도 잃지 않게.

---
---

# 🔵 **5번 세션에게 — 추가 인계** (2026-08-05 11:5x KST)

> 사장님이 5번에게 이렇게 말씀하셨습니다.
> **「네가 할 일은 영어뉴스 + 데이터가공이다. K팝 등에 관심이 많은 해외 대상이다」**
>
> 그리고 저에게: **「실수 말고 누락 안 하고 제대로 작업지시서 만들어 내 손이 안 가게 할 것.
> **공동으로 쓰는 리소스**에 대한 것, **업무보고**, 마지막으로 **공동작업한 비즈니스 관련
> 내용**도 인계해라. 처음 3일은 네가 직접 지시한다」**
>
> 위(Ⅰ~Ⅹ)에 빠진 세 가지를 여기 채웁니다. **Ⅺ·Ⅻ·ⅩⅢ 을 반드시 읽으십시오.**

---

## Ⅺ~ⅩⅢ. → **`docs/세션-공통수칙.md` 으로 옮겼습니다**

공동 리소스 · 업무보고 · 사장님 요청 · 라이선스 · 비즈니스 · 일하는 법은
**3·5·6번이 같은 문서를 봅니다.**

🔴 **같은 규칙을 두 군데 적으면 어느 쪽이 맞는지 아무도 모릅니다.**
   2026-08-05 하루에 제가 그 실수를 **네 번** 했습니다. 여기서 또 하지 않겠습니다.

**👉 `docs/세션-공통수칙.md` 를 먼저 읽으십시오.**

---

## ⅩⅣ. 이름을 바꿉니다 — 당신은 **5번**입니다

이 문서 앞부분(Ⅰ~Ⅹ)의 「위키팁 세션」은 전부 **5번**으로 읽으십시오.
세션 번호: **1번 klifemap · 2번 SeoulMarkets(나) · 3번 백년지도 · 4번 1번 보조 · 5번 당신(WikiTip)**

사장님이 5번에게 하신 정의를 그대로 옮깁니다.

> **「네가 할 일은 영어뉴스 + 데이터가공이다. K팝 등에 관심이 많은 해외 대상이다」**

**「영어뉴스」가 앞에 있습니다.** 데이터를 모으는 것으로 끝내지 말고
**영문 기사로 나가야** 합니다. 기사 꼴은 `content/articles/` 의 것을 보고 따르십시오
(frontmatter 에 출처·교차검증·제외 항목·정정 이력이 들어갑니다).
