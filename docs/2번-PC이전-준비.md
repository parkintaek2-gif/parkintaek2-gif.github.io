# 노트북 → 데스크탑 이전 준비 (2026-08-21 · 2번)

> 사장님 (2026-08-21): **「서버로 쓸 데스크탑 PC 메모리 32기가로 늘림. 슬롯이 두 개만 있었다」**
> **「오늘 노트북에서 pc로 이전하게 준비해」**

아래 숫자는 **오늘 이 노트북에서 잰 것**입니다. 짐작이 아닙니다.

---

## 0. 🔴 제일 먼저 정할 것 — **새 PC 의 사용자 이름**

```
지금 모든 길이 C:\Users\USER\... 로 박혀 있습니다
  · 창 여는 .cmd 아홉 개          set CLAUDE_CONFIG_DIR=C:\Users\USER\.claude-uN
  · 배포 락                       C:\Users\USER\Documents\GitHub\.cloudtype-deploy-lock.json
  · 대화 기록 폴더 이름           C--Users-USER-Desktop  ← 이름 자체가 «길»이다
```

🔴 **새 PC 의 계정 이름이 `USER` 가 아니면 세션이 「대화가 없다」며 새로 시작합니다.**
   `--resume` 은 `projects/<cwd를 글자로 바꾼 이름>/` 을 봅니다. 길이 바뀌면 그 폴더를 못 찾습니다.

```
✅ 가장 안전   새 PC 에 계정을 «USER» 라는 이름으로 만든다 (길이 하나도 안 바뀐다)
△  차선       이름이 다르면 접합점을 만든다
              mklink /J "C:\Users\USER" "C:\Users\<진짜이름>"
              ⛔ 접합점은 되지만 «두 길이 같은 곳을 가리키는» 상태라 나중에 헷갈립니다
⛔ 하지 말 것  길을 손으로 하나씩 고치기 — 아홉 자리에 흩어져 있어 반드시 하나를 빠뜨립니다
```

**사장님께 여쭐 것은 이 한 줄뿐입니다 — 새 PC 계정 이름을 `USER` 로 해도 됩니까.**

---

## 1. 옮길 것 — **잰 크기**

| 무엇 | 크기 | 왜 옮기나 |
|---|---|---|
| `C:\Users\USER\.claude\projects` | **1.6 GB** | 아홉 자리의 **대화 기록**. 이것이 없으면 모든 세션이 기억을 잃는다 |
| `.claude-u3` · `-u4` · `-u5` · `-u6` | 0.2+0.2+0.3+0.1 = **0.8 GB** | 팀 자리 넷의 계정·기록 |
| `C:\Users\USER\.claude.json` | 60 KB | 자리·신뢰 설정 |
| `Desktop\00_세션입구\` | 0.02 GB | 창 아홉 개를 여는 .cmd · 훅 · `_현재\N.id` |
| `GitHub\dataeconomics` (archive 뺀 것) | **0.30 GB** | 저장소 — git 에 있는 것은 clone 으로도 된다 |
| `GitHub\klifemap` | 0.77 GB | 〃 |
| `GitHub\_tools` | 0.53 GB | ⛔ **git 저장소가 아니다** — 반드시 복사해야 한다(piper 음성) |
| `.env` 둘 | 34 줄 | ⛔ **git 에 없다.** 잃으면 열쇠 26개를 다 다시 받아야 한다 |
| `Desktop` 의 일감 폴더들 | — | `6번 서울마켓 종합 폴더` · `작업공유` · `팀자리` · `중부매일…` |
| `GitHub\dataeconomics\archive` | **4.92 GB** | 🟡 급하지 않다. **나중에 따로** 옮겨도 된다 |

### ⛔ git 에 없어서 «복사 안 하면 사라지는» 것 — 이것이 이번 이전의 진짜 위험입니다

```
.env                      열쇠 26개 (KOSIS · DART · WORK24 6개 · KDI 6개 · NEIS 2개 · RIOT · S3 5개 …)
_tools/                   piper 음성 — 저장소가 아니다
_칸age32/ · .ganglyeong/  중간 산물
docs/취재/                취재 자료
kcw-shorts.mp4 · kcw-shorts-brands.mp4 · 종로.mp4   숏영상 원본
public/og/…png 외 커밋 안 된 17개
```
✅ **커밋 안 된 17개는 오늘 안에 커밋해서 위험을 줄입니다**(제 몫).

## 2. 옮기지 않는 것 — 새 PC 에서 다시 만든다

```
node_modules/      npm ci   ← 복사하면 오히려 깨진다(윈도 네이티브 모듈)
dist/ · out/ · .astro/     빌드하면 다시 나온다
```

## 3. ⛔ 복사로 안 되는 것 — **손으로 다시 해야 한다**

```
① Chrome — claude.ai 로그인 다섯(admin·u3·u4·u5·u6) + Claude 확장
   ⛔ 프로필 폴더를 복사해도 잠금이 기계에 묶여 있어 잘 안 붙는다. 그냥 다시 로그인한다.
   ⭐ 방법은 오늘 찾아 뒀다 — 승인 단추에 **초점을 주고 Enter**. 클릭은 창 배율 때문에 안 닿는다.
② 매시 예약 — 예약은 «세션 안»에만 산다. 창을 새로 열면 **각 자리가 스스로 다시 건다**
③ Cloudtype 로그인 (배포)
```

## 4. 순서 — 이대로 하면 됩니다

```
① [노트북] 커밋 안 된 것 커밋 · 다섯 자리 다 push          ← 오늘 제가 합니다
② [노트북] .env 둘과 _tools 를 «따로» 챙긴다               ⛔ git 에 없다
③ [새 PC] 계정 이름 USER 로 만든다 · Node 24.18 · git · Claude Code 2.1.237
④ [새 PC] git clone 넷 → npm ci
⑤ [전송]  아래 셋만 통째로 옮긴다 (합 2.4GB · USB 나 공유폴더)
             C:\Users\USER\.claude\
             C:\Users\USER\.claude-u3 ~ -u6\  · .claude.json
             C:\Users\USER\Desktop\00_세션입구\
⑥ [새 PC] .env 둘을 제자리에 놓는다
⑦ [새 PC] Chrome 로그인 다섯 · Cloudtype 로그인
⑧ [새 PC] 창 아홉 개를 연다 → 각 자리가 매시 예약을 다시 건다
⑨ [노트북] 창을 다 닫는다   ⛔ 이게 마지막이다. 두 기계가 같이 돌면 커밋이 부딪친다
```

⛔ **⑨를 먼저 하지 마십시오.** 새 PC 가 돌기 전에 노트북을 끄면 그 사이 일이 멈춥니다.
⛔ **두 기계에서 같이 돌리지 마십시오.** 같은 저장소에 두 벌이 밀면 메모가 충돌합니다
   (8/9 에 `git add -A` 하나로 남의 파일 6MB 를 쓸어간 사고와 같은 뿌리입니다).

## 5. 옮긴 뒤 **잰다** — 이 여덟 칸이 다 초록이어야 끝난 것이다

```
□ 창 아홉이 각각 «제 대화»를 이어서 연다 (새 대화가 아니다)
□ 자리 넷의 계정   claude auth status 가 u3·u4·u5·u6 을 낸다
□ 관문            node scripts/gate.mjs  → 빨강 0
□ 배포 열쇠       node scripts/deploy-key.mjs 가 물음을 낸다
□ 사이트 넷 200   klifemap.ai · 100yearmap.com · kculturewire.com · seoulmarkets.com
□ .env            node -e 로 열쇠 26개가 다 읽히나 (값은 안 찍는다)
□ piper 음성      _tools 로 소리 하나가 나오나
□ 매시 예약       아홉 자리가 «다시 걸었다»고 메모에 적었나
```

🔴 **「옮겼다」와 「돌고 있다」는 다릅니다.** 오늘 5번에서 그것을 봤습니다 —
   계정은 12:55 에 붙었는데 창은 13:46 까지 예전 폴더로 돌고 있었습니다.

## 6. 🖐 사장님 손

```
① 새 PC 계정 이름을 «USER» 로 — 이것 하나만 정해 주시면 나머지는 제가 합니다
② Chrome 로그인 다섯 · Cloudtype 로그인 (비밀번호라 제 손이 아닙니다)
③ 옮길 길 — USB 로 할지, 두 PC 를 같은 공유폴더로 묶을지
```

## 7. 새 기계의 한계 — **32GB 가 끝값입니다**

```
슬롯 2개 = 16GB×2 로 다 찼다 → 더 늘리려면 두 장을 «버리고» 32GB 두 장을 산다(교체다)
```
⛔ 「램이 두 배니 세션도 두 배」로 세지 않습니다. 8/7 에 잰 것은
   **claude 프로세스 39개가 8.7GB** 였습니다 — 램을 먹는 것은 세션 수가 아니라 프로세스 수입니다.
✅ 옮긴 뒤 그 기계에서 **다시 재고** 자리 수를 정합니다.
