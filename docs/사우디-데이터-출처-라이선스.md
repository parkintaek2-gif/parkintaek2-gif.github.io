# 사우디아라비아(Tadawul) 데이터 출처 라이선스 — 1번, 2026-09-15

## 결론 요약

```
⛔ saudiexchange.sa(Tadawul 웹사이트) 를 스크레이핑해 우리 저장소에 담는 것 — 금지 확인됨
✅ 사우디 CMA 오픈데이터(cma.gov.sa) — 실제로 열려 있다. 공식 API·통계 Excel 다 있다
⬜ 다만 «상장사 목록(종목코드·영문명·업종·상장일)» «그 자체»는 이 API·통계표에서 못 찾았다
⛔ open.data.gov.sa(SDAIA) — 이번엔 접속 자체가 안 됐다(net::ERR_CONNECTION_TIMED_OUT). 못 쟀다
```

🟢 **[2026-09-20 정정] 위 줄을 「상장사 명부 자체가 없다」로 읽지 마십시오.**
  명부는 **있습니다** — Tadawul 제 사이트가 아닌 다른 우물에 있습니다.
  위키데이터가 Tadawul 상장사 **185곳(종목코드 183개)**을 CC0 으로 내고, OpenFIGI 도
  사우디 거래소 종목을 냅니다. 실측은 맨 아래 「다시 쟀다」 절에 있습니다.

### [2026-09-15 17:2x 추가] CMA 오픈데이터 실측

크롬 원격디버그로 `cma.gov.sa/en` → 「Open Data Library」(cma.gov.sa/en/AboutCMA/
ResearchAndReports/opendata/Pages/default.aspx)를 직접 열었다. 실제로 있다 —

```
✅ 공식 REST API   https://opendataapi.cma.gov.sa/swagger/index.html (swagger.json 응답 200)
   엔드포인트 4개 — CRACompanies(신용평가사) · Licenses(인가기관) · PrivateFunds · PublicFunds
   ⛔ 상장 «주식회사» 목록(종목코드 단위)은 이 API 에 없다. 인가기관·펀드·평가사 축뿐이다
✅ 통계 Excel 8종   Statistical Bulletin-Equities·Fintech·Sukuk&Bonds·Mutual Funds·
                  Corporate Governance·Securities Offering·Capital Market Institutions·
                  Annual Report Statistical Appendix (전부 분기·연 단위, «Excel 파일로 전체를 낸다»
                  고 페이지에 명시)
   ⛔ 다 «집계 통계»다(부문별 매출·순이익·소유구조 비율 등) — 종목 하나하나의 코드·영문명·
     업종·상장일을 나열한 «명부(roster)»가 아니다. 5번이 정한 1단계 산출물과 결이 다르다
⬜ Terms of use    아직 원문을 못 열었다(링크를 못 찾음) — 다음에 마저 본다
```

`open.data.gov.sa`(SDAIA 국가 포털)는 이번 실측에서 **접속 자체가 20초+ 타임아웃**으로
막혔다 — 지역 차단인지 서버 문제인지는 못 갈랐다. 「없다」가 아니라 「못 쟀다」다.

## 무엇을 확인했나

`docs/세션간-메모.md`「[5번 → 1번·3번·5번·6번] 2026-09-14 19:2x」로 사우디(Tadawul)를
배정받아 첫 산출물(상장사 목록)을 만들려고 했다. `saudiexchange.sa` 의 Main Market Watch
지면(로그인 없이 공개)에서 실제로 272개사(부문 22개, `noOfSymbolsListedMain: 272`와 일치)를
받는 데까지는 **기술적으로 성공**했다 — 다만 이 사이트가 plain curl·fetch 를 403 으로 막아,
CLAUDE.md 「9222 로 붙는다」 절대로 실제 크롬(원격 디버그)으로 열어야 했다.

**저장하기 전에 약관을 봤다** (WebSearch 로 saudiexchange.sa 의 legal_notice 를 찾음):

> 「You must not reproduce or store any part of the website or its content in any other
>  website or include it in any public or private electronic retrieval system or service
>  without the prior written consent of Tadawul.」

**정확히 이 자가 하려던 일을 금지한다.** `docs/UAE-데이터-출처-라이선스.md` 1-1절의 ADX 판정
(「재배포·조직적 수집(scraping)·파생물 제작을 명시적으로 금지한다… 이 경로는 쓸 수 없다」)과
글자까지 닮았다 — 같은 종류의 사이트(거래소 자체 시황 화면)는 같은 종류의 약관을 쓴다.

⛔ robots.txt 는 직접 못 읽었다(그 요청도 403) — 「robots.txt 는 열려 있다」로 짐작하지 않는다.

## 어떻게 풀었나 (UAE 선례) — 사우디도 같은 길을 따라간다

UAE 는 거래소(ADX) 자체 웹사이트가 막히자, **거래소가 아니라 «규제기관»의 오픈데이터**로
갈아탔다 — UAE CMA(Capital Market Authority)·CBUAE(중앙은행)가 `open-data` 정책을 공개
운영하고 있었고, 그중 실제로 종목 단위 데이터가 있는 CBUAE FX/EIBOR 축으로 착수했다.

사우디에서 같은 자리에 있을 만한 곳(**확인 안 됨, 다음 사람이 확정한다**):

```
사우디 CMA           cma.gov.sa — UAE 의 CMA 와 «이름만 같고 다른 기관»이다. 헷갈리지 않는다.
                     오픈데이터 정책·상장사 데이터셋이 있는지 아직 못 봤다.
Saudi Open Data      open.data.gov.sa — SDAIA(사우디데이터인공지능청) 운영.
Platform             2025-06 기준 289개 기관·11,439개 데이터셋(WebSearch로 확인, 사이트 직접 확인 아님).
                     자본시장 관련 데이터셋이 있는지, 종목 단위인지는 아직 못 봤다.
```

## 남긴 것

- `scripts/collect-saudi-tadawul-companies.mjs` — 표 파싱 로직(자가시험 11건 통과)은 «크롬으로
  이 지면을 읽을 수 있다»는 기술 증거로 남긴다. `--적는다`는 코드로 막아 뒀다(문장이 아니라
  검사로 지킨다) — 이 판정이 뒤집히기 전까지 실제로 아무것도 저장되지 않는다.
- `archive/raw/saudi-tadawul-companies/` — 약관 확인 «전»에 한 번 받아 뒀던 것은 지웠다.

---

# 🟢 [2026-09-20 · 5번] 「명부 자체가 없다」를 **다시 쟀다 — 있다**

사장님이 아시아 확장 주인을 다시 정하시면서 이 자리가 제게 왔습니다. 우리 규칙은
**「없다고 보고하기 전에 그쪽 화면에 그 수가 떠 있는지 본다」**이고, ADX 시가총액에서
같은 잘못을 한 번 했습니다 — **우리가 못 찾은 것을 그쪽이 안 내는 것으로 적었습니다.**
그래서 판정을 다시 쟀습니다.

## 오늘 실측 (2026-09-20 07:5x)

```
우물                          결과                                     판정
GLEIF LEI (api.gleif.org)     사우디 «법인» 6,027곳 · 200 · 1.6초       ✅ 열림 · CC0
GLEIF ISIN↔LEI 매핑 (일일 zip) 전체 929만 행 중 SA 로 시작하는 ISIN 0건  ⛔ 사우디는 안 싣는다
위키데이터 SPARQL             Tadawul 상장사 **185곳 · 종목코드 183개**  ✅ 열림 · CC0
OpenFIGI /v3/search (AB)      200 · 100건씩 쪽넘김(next) · 열쇠 없이 됨   ✅ 열림
SDAIA open.data.gov.sa        node fetch 실패 · 크롬으로도 안 열림        ⬜ **또 못 쟀다**
SAMA (옛 주소)                404                                       ⬜ 주소를 다시 찾아야 한다
```

## ⇒ 결론이 바뀐다

**「상장사 명부 자체가 없다」는 틀렸습니다.** 명부는 있습니다 — 다만 **Tadawul 제 사이트가
아니라 다른 우물**에 있습니다.

```
⛔ 그대로 맞는 것   saudiexchange.sa 약관이 저장·재배포를 금지한다 (1번 판정 유지)
✅ 새로 찾은 것     위키데이터가 Tadawul 상장사 185곳을 종목코드와 함께 CC0 로 낸다
                   OpenFIGI 가 사우디 거래소 종목을 이름과 식별자로 낸다(열쇠 없이)
                   GLEIF 가 사우디 법인 6,027곳을 CC0 로 낸다(법인명·주소·상태)
⬜ 아직 못 잰 것    SDAIA 포털 — 두 번 다 못 열었다. 「없다」가 아니라 「못 쟀다」다
```

⚠ **185곳은 전부가 아닙니다.** Tadawul 본시장은 272곳이므로 위키데이터는 약 68%입니다.
  그 수를 «전부»인 것처럼 적지 않습니다 — 지면에 낼 때 **몇 곳을 덮는지 함께 적습니다.**

## 그래서 사우디를 여는 길 (제안 · 5번)

```
1단계  위키데이터 185곳 + OpenFIGI 로 «종목 명부»를 만든다 — 둘 다 재배포가 열려 있다
       ⚠ 겹치는 곳을 종목코드로 맞춰 보고, 어느 쪽에만 있는지를 «세어서» 적는다
2단계  GLEIF 로 법인명·주소·상태를 붙인다(같은 회사인지 LEI 로 묶는다)
3단계  재무제표 — 아직 열린 우물을 못 찾았다. CMA 통계 Excel 을 «실제로 내려받아»
       종목별 탭이 있는지 본다(아래 3번, 아직 안 했다)
⛔ 「사람(임원·근속)」을 1단계에 두지 않는다 — 재무·공시가 먼저다(CLAUDE.md 「주력과 서비스」)
```

⛔ **오늘은 아무것도 저장소에 쌓지 않았습니다.** 재기만 했습니다.

---

## 다음 사람이 할 일

1. ~~`cma.gov.sa` 열어 확인~~ — **끝냈다(위 17:2x절).** API·통계표는 있으나 종목 명부는 없다.
2. `open.data.gov.sa`(SDAIA) 를 다시 시도한다 — 이번엔 타임아웃으로 «못 쟀다». 시간을 두고
   다시 열거나, 다른 경로(예: 검색엔진 캐시·미러)로 그 포털의 사우디 자본시장 데이터셋
   존재 여부만 먼저 본다.
3. **CMA 통계 Excel(예: Statistical Bulletin-Equities)을 실제로 «열어» 본다.** 집계표 안에
   부문별 합계 말고 «종목별 원자료 탭」이 숨어 있을 가능성이 있다(엑셀은 웹페이지보다
   자료가 더 실릴 때가 많다) — 이번엔 페이지 설명 문구만 읽었지, 실제 파일을 내려받아
   펼쳐 보지는 못했다.
4. 그래도 종목 단위가 없으면(UAE CMA 가 그랬듯) 매크로 축(SAMA 의 환율·금리 등)부터
   먼저 열 수 있는지 본다 — 한국·UAE 가 시작한 자리와 같다.
5. 확정되면 이 문서 결론 요약을 고치고, `scripts/collect-saudi-tadawul-companies.mjs` 의
   차단(`process.exit(1)`)을 다른 소스를 향하도록 다시 짠다 — 이 파일의 표 파싱 로직 자체는
   재사용 가치가 없다(다른 사이트면 표 구조가 다르다).
