# 개봉날 차례표 — 3번(백년지도) · **2026-08-15 아침**

> 2번 지시(04:1x) — *「그날 아침에 켤 것을 그날 정하면 반드시 빠뜨립니다.
> ③(되돌리는 것)이 제일 중요합니다 — **창이 죽으면 적어 둔 것만 남습니다**」*

⛔ 계획서가 아니다. **그날 아침에 그대로 따라 하는 차례표**다.
⛔ 오늘 켜지 않는다. 적어만 둔다.

---

## ✅ 먼저 — **사는 길은 났다. 이제 정말 「한 줄」이다** (2026-08-10 09:3x 고쳐 적음)

> ⚠ 이 절에 어제 *「그 화면이 지금 없다」*고 적혀 있었다. **오늘 만들었다.** 그 말은 거둔다.
> ⛔ 옛말을 지우지 않고 고쳐 적는 까닭 — 이 문서는 **창이 죽어도 남는 것**이라
>   「없다」를 그대로 두면 그날 아침에 없는 것을 또 만들게 된다.

**2번이 확정했다(10:05)** — *「KLifeMap 결제로 넘기는 것 맞습니다. 끈 채로 두는 것이 맞습니다」*

> 🔴 **켜는 조건이 바뀌었다** (2026-08-10 12:0x). 2번이 나에게 준 것은 *「계좌이체가 켜지는 때」*였는데,
> 그 뒤 **사장님이 자체 계좌이체를 빼셨다.** 두 자리가 따로 같은 말을 적었다 —
>
> ```
> 6번  「사장님이 자체 계좌이체 빼심 → 4곳 PG 뒤로 정정」          (05a5d32)
> 1번  「연결 조건이 바뀌었다 — PG 전에는 값 단추를 켜도 못 산다」  (bbe0131)
> ```
>
> ⛔ 그래서 **켜는 조건은 「계좌이체」가 아니라 「PG」다.** 아래 표도 그렇게 고쳤다.
> ⚠ 2번이 한 줄로 확정해 주시면 이 단서를 뗀다. 그전까지는 **남의 자리에서 옮겨 온 말**이다.

### 🔴 확인 셋 — **2번이 적어 두라 한 것. 잰 값 그대로다**

```
① 살수있나 가 꺼져 있나
   ✅ 꺼져 있다 — src/lib/price.ts 「살수있나: false」

② 켰을 때 어디로 가나
   /price          https://klifemap.ai/checkout.html?type=single&service=regionSet
                   &from=100y&at=price
   지역 한 벌       위와 같고 뒤에 &area=서울특별시 노원구   ← 어느 벌인지 실어 보낸다

③ 딱지가 거기까지 사나
   ✅ 산다 — 200 이고 **되보내기가 없다**. 물음표 뒤가 그대로 도착한다
```

### ⚠ 켜기 전에 남의 손 — **이 둘이 없으면 켜도 오류 화면이다**

```
🖐 사장님   **PG**                          🔴 자체 계좌이체는 사장님이 빼셨다(8/10).
            그 전까지는 결제 수단이 **하나도 없다** — payMethods.bankTransfer = false
🖐 1번      KLifeMap 요금표에 한 줄            ⏸ **PG 날 함께 넣는다**(1번 답 8/10 16:1x).
            지금 넣으면 1번 자물쇠가 선다 — 값표에 있는 것은 화면에서 살 수 있어야 한다
                                              (있는 것: saju · sajuAiReport · gunghap ·
                                               taekil · sungmyung · careerCoach ·
                                               parentCoaching · childCareerCoaching ·
                                               coupleLoverCoaching · separationCoaching ·
                                               personalityCoaching · lifeDesignCoaching ·
                                               intelligenceOne · intelligenceAll)
```
⛔ 없는 상품으로 보내면 checkout 이 **「잘못된 주문」 칸**을 띄운다. 막다른 길보다 나쁘다.
⚠ 그리고 checkout 은 **로그인을 요구한다**(`gateLoggedOut`). 백년지도 손님은 계정이 없다.
  이걸 어떻게 할지는 **2번 답을 기다리는 중**이다.

### ⭐ 이번엔 「켜면 화면이 선다」를 **켜서 빌드해 확인했다**

```
살수있나 false   「곧 엽니다」+알림 메일 그대로 · 사는 단추 없음        ✅ 빌드본에서 확인
살수있나 true    /price 와 노원구 **둘 다** 사는 단추가 서고
                 「곧 엽니다」가 사라짐                                 ✅ 빌드본에서 확인
```
⛔ 확인 뒤 **도로 껐고** `dist` 도 꺼진 꼴로 다시 빌드해 두었다.

---

## 🔴 8/15 에 여는 것은 **결제가 아니다** (2026-08-10 12:3x · 2번이 사장님 말씀을 옮겨 옴)

```
사장님 — 「계좌이체 서비스 하지마. PG 붙이면 오픈한다.
          무료서비스 부분 / 회원가입 / 로그인은 오픈」
```

**그래서 8/15 에 백년지도가 여는 것은 이것뿐이다 — 잰 값 그대로**

```
무료서비스     ✅ **이미 다 열려 있다.** 지면 4,971장 중 값을 받는 것은 지역 한 벌 114장뿐이고,
                 그마저 지금은 「곧 엽니다」다. 나머지는 오늘도 무료로 열려 있다
회원가입·로그인 🔴 **백년지도에는 없다. 그리고 없는 것이 맞다** —
                 /login /signup /join /register /my /account /mypage → **일곱 다 404**
                 ⭐ 첫 화면이 손님에게 **「무료입니다. 가입도 필요 없습니다」**로 인사한다
                 ⛔ 여기에 가입을 만들면 그 인사와 지면 문구가 어긋난다
결제           ⏸ **PG 뒤**다. 8/15 에는 없다
```

⭐ **그러면 사장님이 말씀하신 「회원가입 / 로그인」은 어디인가 — KLifeMap 이다.** 재 봤다.
```
klifemap.ai/login.html   ✅ **200** · 이메일 로그인 + 회원가입이 **한 지면**에 있다
                         입력칸 10개 · 구글 · 네이버 · 카카오 · 이메일 인증번호
                         부르는 문 — /api/auth/login · signup · email · me · providers
⛔ /signup.html · /join.html · /register.html 은 404 다 — **가입은 login.html 안에 있다**
⛔ 계정을 만들어 보지는 않았다(집 규칙: 가입·본인인증을 하지 않는다). **열리는 것까지** 쟀다
```
🔴 **그 지면에 이 줄이 있다 — 「만 14세 미만은 가입할 수 없습니다」.**
  ⚠ 우리 손님의 절반은 **고등학생**이다. 결제를 KLifeMap 으로 넘기기로 한 이상,
    **고1(만 15~16세)은 되고 중3 이하는 안 된다.** 이건 값보다 앞선 자리라 적어 둔다.
  ⭐ 우리 강령이 *「보호자 동석이 구조다」*이니 **어긋나지는 않는다.** 다만 **알고 있어야 한다.**

**⛔ 그러니 개봉날 내가 켤 것은 없다.** 무료는 이미 열려 있고, 가입·로그인은 1번 자리다.
  내 몫은 **③(스위치)을 PG 가 붙는 날까지 안 켜는 것**과 **② 재는 것 일곱 줄**이다.

---

## ① 켜는 것 — **여덟 줄** (⚠ 1·3 은 **8/15 가 아니라 PG 뒤**다)

| # | 무엇 | 누구 | 어떻게 |
|---|---|---|---|
| 1 | ~~계좌 세 값~~ → **PG** | 🖐 **사장님** | ⏸ **8/15 아님. PG 붙는 날이다.** 그 전에는 결제 수단이 하나도 없다 |
| 2 | ~~살 수 있을 때 보여 줄 **화면**을 붙인다~~ | ~~3번~~ | ✅ **8/10 에 끝냈다**(`12794e3`). 두 지면이 `다음칸.살수있나` 로 갈린다 |
| 2′ | KLifeMap 요금표에 **`regionSet` 9,900원 한 줄** | 🖐 **1번** | ⏸ **PG 날 함께 넣는다.** 1번 답(8/10 16:1x) — 지금 넣으면 1번 자물쇠(`product-sellability`)가 선다. ⭐ 그날 「손님이 내면 무엇을 어디서 받나」 한 줄을 내가 준다 |
| 3 | `src/lib/price.ts` **`살수있나: false → true`** | 3번 | ⏸ **8/15 에 안 켠다.** ①(PG)과 2′ 뒤다. ⭐ 이 줄이 정말로 화면을 바꾼다(빌드로 확인함) |
| 4 | klifemap `/child-career.html` 의 「**결제는 정식 오픈 뒤에 열립니다**」를 바꾼다 | 🖐 **1번** | ⛔ 3번 지면과 **같이** 바꾼다. 한쪽만 바뀌면 지면끼리 말이 갈린다 |
| 5 | 네이버 서치어드바이저 등록 | 🖐 **사장님** | 우리 준비물(robots·사이트맵·소유확인)은 다 됐다 |
| 6 | ⛔ ~~애드센스에서 `a100yearmap.com` 지우고 `100yearmap.com` 넣기~~ | ⏸ **멈춤** | 🔴 **8/10 에 심사가 들어갔다.** 화면을 누르면 **심사가 처음으로 돌아간다.** 심사가 끝난 뒤로 미룬다 |
| 7 | 유튜브 `out/서른둘-소리.mp4` 올리기 | 🖐 **사장님** | 붙일 글은 `docs/3번-영상-대장.json` 안에 있다 |
| 8 | **유튜브 설명 세 줄을 짧은 주소로** (`/y/jongno` · `/y/kyungshin` · `/y/home`) | 🖐 **사장님** | 2번이 문안을 올린다. ⛔ 지금 주소는 한글이 들어 **잘려 보인다** |

```
남이 해 줘야 하는 것 — **여섯 개** (🖐 1번 둘 · 🖐 사장님 넷)
   🖐 1번   ④ klifemap 문구      (2′ 요금표는 **PG 날**로 옮겼다)
   🖐 사장님 ① **PG**   ⑤ 네이버   ⑦ 유튜브 올리기   ⑧ 유튜브 설명을 짧은 주소로
   ⏸ ⑥ 애드센스는 **빠졌다** — 8/10 에 심사가 들어가 아무도 못 건드린다

🔴 8/15 에 필요한 것은 **⑤⑦⑧ 셋뿐**이다.
   ①(PG) 과 2′(요금표) 은 **켜는 조건**이지만 **그날이 아니다.** 붙는 날 함께 한다
   ⛔ 둘 중 하나만 없이 ③(스위치)을 켜면 손님이 「잘못된 주문」 화면을 본다
```

**배포는 자물쇠 순서로만 한다 — ⛔ 이 순서를 그날 줄이지 않는다**
```
node scripts/deploy-key.mjs                      → 되돌아간것.tsv 를 읽고 답한다
node scripts/deploy-key.mjs --답 "…"              → 열쇠
node scripts/check-deploy-ready.mjs --열쇠 <열쇠>   → 관문 네 항
node scripts/deploy.mjs --열쇠 <열쇠>              → 배포
```

---

## ② 재는 것 — **일곱 줄. 켠 뒤에 바로**

```
1  /price 가 200 이고 「아직 결제를 받지 않습니다」가 **사라졌나**
2  파는 지면 한 장(노원구)에 **사는 길**이 보이나 — 폰 375px 로 본다
3  값이 한 곳에서 오나 — 지면 두 곳의 값이 **둘 다 9,900원**인가
4  문이 **스물두 갈래** 다 나오나 — ⚠ 8/10 저녁에 열여덟에서 늘었다(price·terms·privacy·refund 넷)
   ⛔ **자가 없다.** src/lib/klifemap.ts 의 `붙일수있는갈래` 를 돌며 `입구주소()` 가
     22/22 로 나오는지 본다. 어른 문 6 · 아이 문 16 이어야 한다
   ⚠ 그런데 **지면에 문을 실제로 다는 것은 18갈래**다. 넷(`문을안다는갈래`)은 딱지만 있고 문이 없다 —
     /price 는 파는 지면이라 밖으로 안 내보내고, 약관 셋에 적성 문은 결이 아니다. **일부러 그렇게 뒀다**
5  폰 첫 화면 — 메뉴가 아직 한 줄인가. 그리고 **「값」 칸이 로고 다음에 있나**(8/10 에 옮겼다)
6  학교 설명에 숫자가 아직 **2,392장**에 있나
   ⚠ 세는 법을 못 박는다 — 설명에 「진학률 %」·「졸업자 명」·「학급당 명」·「학업중단 %」
     **넷 중 하나라도** 있는 지면을 센다. ⛔ 「숫자가 있나」로 세면 2,459 가 나온다(8/10 에 내가 그렇게 틀렸다)
7  조사가 아직 맞나 — /school/7801089 이 **「삼척시는」**인가(8/10 에 489장 고쳤다)
```
⛔ 「배포했다」로 ✅ 를 치지 않는다. **라이브에서 다시 재야** ✅ 다.
⚠ 자가 「못 쟀다」로 끝날 수 있다(새 지면 0장인 날). 그러면 **내가 라이브를 직접 재서** 적는다.

---

## ③ 🔴 되돌리는 것 — **제일 중요하다. 창이 죽어도 이 줄만 있으면 된다**

```
무엇이 잘못되면            어떻게 되돌리나
──────────────────────────────────────────────────────────────
값 화면이 이상하다          src/lib/price.ts 의 살수있나 를 **false** 로 되돌리고 배포
                          (자물쇠 순서 그대로. 열쇠는 커밋이 바뀌었으니 **다시 받는다**)
지면이 깨졌다              git revert <그 커밋> · git push · 배포
                          ⛔ git reset --hard 를 쓰지 않는다. 남의 커밋이 같이 날아간다
무엇이 나갔는지 모르겠다     git log --oneline -10 -- src/lib/price.ts
                          그리고 **라이브를 연다** — 저장소가 아니라 화면이 참이다
배포가 「못 쟀다」로 끝났다   실패가 아니다. ⛔ 다시 돌리지 말고 **라이브를 직접 잰다**
창이 죽었다                ⭐ 창이 열리면 **가장 먼저 매시 예약을 다시 건다**(:12 · :42)
                          그다음 이 문서를 연다
커밋 안 한 것이 있었다       ⛔ 창이 죽으면 커밋 안 된 것은 통째로 없어진다.
                          그래서 **켜기 전에 커밋부터** 한다
```

**되돌릴 수 없는 것 — ⛔ 미리 안다**
```
🖐 사장님이 유튜브에 올린 것 · 네이버에 등록한 것 — 우리가 못 되돌린다
⚠ 그래서 그 넷(⑤⑥⑦⑧)은 **값 화면이 선 다음**에 부탁드린다. 순서를 바꾸지 않는다
```

---

## 그날 아침 순서 한 줄로

```
【8/15 아침 — 결제가 없는 날】
커밋 → 자물쇠 배포 → ② 일곱 줄 재기 → 🖐 사장님 ⑤⑥⑦⑧
      ⛔ ③(스위치)을 **켜지 않는다.** 그날은 무료만 연다
      ⭐ 내가 켤 것이 없다는 것이 이 문서에서 제일 중요한 한 줄이다

【PG 가 붙는 날 — 나중이다】
🖐 사장님 ①PG · 🖐 1번 2′요금표  →  3번 ③스위치 한 줄 → 자물쇠 배포 → ② 다시 재기
      → 다 맞으면 🖐 1번 ④(klifemap 문구를 같이 바꾼다)
      → 하나라도 틀리면 ③ 으로 간다
🔴 ①(PG)과 2′ 이 **내 손보다 앞이다.** 둘이 안 되어 있으면 ③(스위치)을 켜지 않는다 —
   켜면 손님이 「잘못된 주문」 화면을 본다. **그건 막다른 길보다 나쁘다**
```
