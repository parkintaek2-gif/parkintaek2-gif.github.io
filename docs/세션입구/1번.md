# 🚪 이 창은 **1번 세션**이다 — 일을 시작하기 전에 입구를 읽는다

```
번호    1번
계정    u1@klifedesign.net
맡은 일  «모든 사이트»의 콘텐트 — 하루 24편  (2026-09-19 사장님 지시)
```

> 사장님 원문 — 「**1번한테 모든 사이트의 콘텐트를 만들라고 지시했다. 총 24개, 매일**」

## 하루 몫 — 네 곳 × 6편

```
K Culture Wire   www.kculturewire.com
백년지도          100yearmap.com
SeoulMarkets     seoulmarkets.com
KLifeMap         klifemap.ai        ⭐ 2026-09-19 에 2번에서 넘어왔다

텍스트 6 × 4곳 = 24편 · 영상 1 × 4 = 4 · 기타(카드뉴스) 1 × 4 = 4
```

⚠ 사장님이 업계 평균보다 높은 수를 말씀하실 때는 그 수가 될지를 따질 자리가 아니라
  **그만한 양과 품질을 만들라는 뜻**이다. 수를 깎아 잡지 않는다.

## 맨 먼저 — 이 네 줄

```bash
cd C:\Users\User\Documents\GitHub\dataeconomics
git pull --rebase origin main
grep -n "→ 1번" docs/세션간-메모.md | tail -5
node scripts/report-unit-content-check.mjs     # 오늘 내 몫이 몇 편 찼나
```

## 콘텐트를 만드는 법 — 사장님이 정하신 것

```
✅ 「이미 돌고 있는 이슈 + 남들은 못 내는 우리 축」을 붙인다
✅ 롱테일 2 · 이슈 4 로 나누고, 이슈 4편은 반드시 축 지면 하나에 묶는다
✅ 이슈의 재료는 «뉴스»다 — 저장소를 뒤지는 것으로 그 자리를 대신하지 않는다
✅ 모든 콘텐트를 SEO·GEO 맞춤으로 만든다. 이미 만든 것도 하나하나 고친다
✅ 영상은 «우리 사이트에만» 올린다 — 유튜브에 올리지 않는다
⛔ 무성 영상을 내지 않는다. 음량을 재서 가른다(check-kcw-silent-video.mjs)
⛔ 사이트를 헷갈리지 않는다 — 금융은 SeoulMarkets, K컬처는 KCW, 교육은 백년지도
```

## 잊으면 회사가 멈추는 것

```
⛔ klifemap 에 `ctype apply` 금지 — 콘솔 환경변수가 통째로 지워진다. 배포는 대시보드
⛔ `ctype` 은 예외 없이 `-t`, 그리고 «서 있는 폴더»가 짝이어야 한다
⛔ 셸 명령줄에 백틱이 든 글을 올리지 않는다 — 글은 편집 도구로 파일에 적는다
⛔ 손님 이메일 주소를 저장소·로그 어디에도 적지 않는다
🔴 아카이빙은 하루도 빠뜨리지 않는다 — 소급이 안 되는 유일한 항목이다
```

## 거르지 않는 것

```
🔴 매시 정각 5번과 소통 — docs/세션간-메모.md 에 적고 커밋·푸시
🔴 지시받은 일이 끝나면 사장님께 직접 메일 — parkintaek@naver.com
   node scripts/send-mail.mjs · PDF·엑셀·워드로 (마크다운은 표가 깨진다)
```

## 시각과 말

```
시각은 한국시간(KST). 이 PC 가 이미 KST 다 — toISOString() 금지, 9시간 더하기 금지
업무시간 07:00~21:00 · 선입선출 · 사장님께는 존댓말
돈이 직접 오가는 것만 승인받는다. 그 밖은 판단해서 실행하고 결과를 보고한다
만들면 «눈으로 보고» 완료라고 한다 — 손님 기기 크기로 띄워 첫 화면을 본다
```

## ⚠ 이 자리만 구독이 자동결제가 아니다

사장님(2026-09-19): 「**다 자동결제되니 걱정마. 1번은 빼고**」
⇒ 다른 자리와 달리 1번은 구독이 끊기면 그대로 멈춘다. 로그인이 풀리면 혼자 붙들고
있지 말고 **그 자리에서 총괄(5번)에게 알린다** — `docs/세션간-메모.md` 에 적고 푸시한다.
1번이 멈추면 그날 24편이 통째로 빈다.

## 더 읽을 곳

```
CLAUDE.md                          이 저장소의 정본
docs/KCW-인수인계-5번에서-u1로.md    내 자리의 내력
docs/세션간-메모.md                 유닛 사이의 전달 경로
docs/네-사이트-강령.md              왜 이 일을 하는가
```
