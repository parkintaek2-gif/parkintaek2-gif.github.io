# 🚪 이 창은 **2번 세션**이다 — 일을 시작하기 전에 입구를 읽는다

```
번호    2번
계정    admin@klifedesign.net   ⭐ 사장님과 함께 쓰는 계정이다
맡은 일  SeoulMarkets 개발  (2026-09-18 사장님 지시)
```

## 맨 먼저 — 이 문서를 연다

```
C:\Users\User\Documents\GitHub\dataeconomics\docs\2번-세션입구.md
```

거기에 지금 잡을 일, 거르면 안 되는 점검, 사고를 내는 자리가 있다.
읽기 전에 손대지 않는다 — 사장님 지시다: 「**뭘 지시하면 일단 히스토리를 봐**」.

## 그리고 이 네 줄

```bash
cd C:\Users\User\Documents\GitHub\dataeconomics
git pull --rebase origin main
grep -n "5번 → 2번" docs/세션간-메모.md | tail -5
git log --oneline --since="12 hours ago" | head -20
```

## 잊으면 회사가 멈추는 것 넷

```
⛔ klifemap 에 `ctype apply` 금지 — 콘솔 환경변수가 통째로 지워진다. 배포는 대시보드
⛔ `ctype` 은 예외 없이 `-t`, 그리고 «서 있는 폴더»가 짝이어야 한다
   SeoulMarkets = dataeconomics 폴더 / 앱 이름 web
   KLifeMap     = klifemap 폴더      / 앱 이름 klifemap-app
⛔ 셸 명령줄에 백틱이 든 글을 올리지 않는다 — 셸이 실행해 엉뚱한 배포가 뜬 적이 있다
⛔ 손님 이메일 주소를 저장소·로그 어디에도 적지 않는다
```

## 거르지 않는 것

```
🔴 KLifeMap 손님길 세 징검다리 — 07:30 · 10:30 · 13:30 · 16:30 · 19:30
   들어오기 · 돈 내기 · 다시 보기. 승인(capture)은 부르지 않는다
🔴 매시 정각 5번과 소통 — docs/세션간-메모.md 에 적고 커밋·푸시
🔴 지시받은 일이 끝나면 사장님께 직접 메일 — parkintaek@naver.com
   node scripts/send-mail.mjs · PDF·엑셀·워드로 (마크다운은 표가 깨진다)
```

## 시각과 말

```
시각은 한국시간(KST). 이 PC 가 이미 KST 다 — toISOString() 금지, 9시간 더하기 금지
업무시간 07:00~21:00 · 선입선출 · 사장님께는 존댓말
돈이 직접 오가는 것만 승인받는다. 그 밖은 판단해서 실행하고 결과를 보고한다
```
