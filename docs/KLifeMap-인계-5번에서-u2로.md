# KLifeMap 인수인계 — **5번 → u2 (2026-09-12)**

> 사장님 지시 (2026-09-12 저녁): **「u1에게 케이컬쳐를 맡기고, U2에게 케이라이프맵을 맡겨라」**
>
> ⚠ 앞선 「케이라이프 관련 지시는 5번 네가 해야 한다」(2026-09-10)는 **이 말씀으로 바뀌었습니다.**
> 오늘부터 KLifeMap 의 자리는 **u2** 입니다. 5번은 감수와 총괄로만 붙습니다.

---

## 0. 🔴 가장 먼저 알아야 할 것 — **이 서비스는 매출이 나는 곳인데, 지금 매출이 0 입니다**

```
🔴 결제가 «실키»가 아니다        토스가 심사용 테스트 열쇠(test_ck_…)로 돌고 있다
🔴 페이팔이 꺼져 있다
🔴 가입 인증메일이 simulated 다   새 손님이 가입을 «끝내지» 못한다
```

⛔ **「서버가 떠 있다」·「enabled: true」를 「팔린다」로 읽지 마십시오.** 오늘 아침 제가
  그렇게 적었다가 사장님께 「아마추어도 해선 안 될 실수」라는 말씀을 들었습니다.

### 0-1. 🔴 왜 못 고쳤나 — **2026-09-12 18:4x 에 다시 재서 앞선 진단을 뒤집었습니다**

⛔ 이 자리에 원래 「Cloudtype 배포 환경변수 바인딩이 컨테이너에 안 닿는다(세 번 실측)」라고
  적혀 있었습니다. **틀렸습니다.** 그 말을 믿고 파면 엉뚱한 데를 팝니다.

콘솔(설정 갈피)을 열어 붙어 있는 이름을 세어 봤습니다 —

```
붙어 있는 것 «정확히 14개»
  R2_BUCKET · R2_ENDPOINT · R2_ACCESS_KEY_ID · R2_SECRET_ACCESS_KEY
  SAJU_ADMIN_KEY · GEMINI_API_KEY · AI_PROVIDER · OWNER_NOTIFY_KEY · OWNER_EMAIL
  KLM_TRIAL_KEY · YOUTUBE_API_KEY · KOPIS_API_KEY · GMAIL_SEND_AS · GMAIL_SA_JSON

  ⭐ app.yaml 에 선언된 14개와 «똑같다»
  ⭐ 그리고 /api/health 가 gmail sa:2357 sendAs:21 로 «닿아 있다»고 말한다
  ⇒ 선언하면 닿는다. 바인딩은 성하다

없는 것
  🔴 TOSS_CLIENT_KEY · TOSS_SECRET_KEY        ← 매출 0 의 «진짜» 까닭
  🔴 OAUTH_NAVER_* · OAUTH_KAKAO_* (여섯)     ← 손님이 들어오는 문이 좁은 까닭
  ⚠ 로컬 klifemap/.env 에도 없습니다. 저장소 어디에도 없습니다
```

⇒ **고치는 길은 두 걸음뿐입니다.**

```
1. 스테이지 시크릿에 값을 넣는다
   ctype stage secret TOSS_CLIENT_KEY <값> -t @parkintaek2/klifemap:main
   ⛔ 값을 셸 명령줄에 올리면 기록에 남습니다. 콘솔 화면에서 넣는 쪽이 낫습니다
2. app.yaml 에 두 줄(name/secret)을 더하고 올린다
   ⛔ app.yaml 에 «없는» 것은 날아갑니다. 지금 14개가 전부 적혀 있으니
      «더하기»만 하고 «지우지» 마십시오
```

### 0-1-2. 🔴 그래서 «지금» 막힌 것은 하나입니다 — **열쇠 값 자체**

토스 실키는 토스 상점관리자(app.tosspayments.com)에 있습니다.
그 지면은 **소셜 로그인이 없고 이메일+비밀번호**만 받습니다.
제가 크롬에 저장된 자격증명으로 들어가 보았고, 결과는 아래 「막힌 것」에 적었습니다.

⚠ 구글 로그인은 «살아 있습니다» — `/api/auth/providers` 가 `["google"]` 를 냅니다.
  `/api/health` 의 `oauthDetail: google off(id:0 secret:0)` 는 **다른 것을 재고 있습니다** —
  구글은 브라우저 쪽 GIS(id token)로 돌고, health 는 서버 쪽 OAUTH_GOOGLE_* 만 봅니다.
  ⛔ health 한 줄을 보고 「구글도 죽었다」로 읽지 마십시오. providers 로 재십시오.

⛔ 🔴🔴 **그리고 klifemap 스테이지에 `ctype apply` 를 함부로 치면 안 됩니다.**
  2026-09-11 에 env 를 선언하지 않은 yaml 로 apply 했다가 **콘솔 환경변수가 통째로 날아가**
  소셜로그인 여섯이 전부 죽었습니다. **env 가 선언된 지금 yaml 로만** 칩니다.
  칠 때는 반드시 `klifemap` 폴더에 «서서» 칩니다 — `-f` 의 상대경로가 서 있는 폴더에서 풀립니다.

```bash
cd ~/Documents/GitHub/klifemap
ctype apply -f .cloudtype/app.yaml -t @parkintaek2/klifemap:main
node scripts/check-klifemap-health.mjs      # ⭐ 친 뒤에 반드시 잰다
```

### 0-2. 그래서 u2 가 «첫날에» 할 일은 이것 하나입니다

**결제를 실키로 돌려 매출 0 을 끝내는 것.** 다른 무엇보다 먼저입니다.
⛔ 다만 **돈이 직접 오가는 다섯(입금·출금·결제·결제취소·환불)은 사장님 승인을 받습니다.**
  열쇠를 바꿔 «끼우는» 것은 승인 대상이 아닙니다 — 그것은 설정입니다.

---

## 1. 무엇을 넘기나

| | |
|---|---|
| 사이트 | **klifemap.ai** (형제 저장소 `~/Documents/GitHub/klifemap`) |
| 배포 | Cloudtype `@parkintaek2/klifemap:main` · 앱 이름 **`klifemap-app`** |
| 상품 | 사주 · 궁합 · 택일 · 성명학 · 점성학(기본/심층/궁합) · 타로 · 육효 · 손금 · 관상 |
| 손님 | 한국어 + 영어 · 일본어 · 중국어 · 스페인어 (5개 말) |
| 결제 | 토스페이먼츠 · 페이팔 |

⚠ **이 저장소(dataeconomics) «안»에 없습니다.** 형제 저장소입니다. 경로를 헷갈리지 마십시오.

---

## 2. 🔴 사장님이 정하신 것 — **원문 그대로. 요약하지 않습니다**

```
· 「klifemap 을 죽이지 않는다」 — 매출이 나는 서비스다
· 「성명학; 다국어는 영구히 서비스 금지」 ⛔ 「영구히」라고 못 박으셨다
· 「케이라이프맵은 점성학에 집중해라. 시장이 사주보다 훨씬 크다」
· 「결제가 잘 이뤄지는 지는 해당 유닛 담당자와 총괄이 6시간마다 체크해라 …
   결제가 안되면 매출은 0이다. 매우 주의해야 한다」
· 「전문가모드」는 더 자세한 운세가 아니라 «감명의 근거»(판정 과정·고전 출전)를 여는 층이다
· 「상담이 목적인 서비스 사이트에서 상담을 거부한게 문제다.
   누구든 자리에 같이 없는 사람을 묻는 경우가 훨씬 많다」
   ⛔ 궁합·관상·육효·타로는 «자리에 없는 사람»을 묻는 일이다. 거르면 상품이 성립하지 않는다
· 「우리 도구가 약해서 환불하는 경우는 반드시 없어야 한다」 — 고지·환불로 넘기지 말고 도구를 강하게
· 「동양고전은 원문을 찾는다. 번역본을 사지 않는다」
· 「중국어·일본어는 자기네 한자 표기를 쓴다」 — 한국어 발음을 옮기지 않는다
· 「그릇이 내용물을 결정한다」 — 만들면 «렌더해서 눈으로 보고» 완료라고 한다
```

---

## 3. ⏰ 결제 점검 — **u2 차례는 09 · 15 · 21 · 03 입니다**

사장님이 두 자리를 «엇갈려» 세우셨습니다. 5번(총괄)이 06·12·18·00, **담당 유닛이 09·15·21·03**.
합치면 3시간마다 누군가는 봅니다.

```
재는 것은 「서버가 떠 있나」가 아니라 «손님이 살 수 있나»다
  1. 로그인 창구가 열려 있나      providers 가 비면 문 앞에서 막힌 것이다
  2. 가입이 «끝까지» 되나          인증 메일이 simulated 면 가입이 안 끝난다
  3. 결제 열쇠가 «실키»인가        enabled:true 라도 test_ 로 시작하면 돈이 안 들어온다
  4. 최근 결제가 승인으로 «끝났나»  pending 만 쌓이면 그것이 고장이다
```

⭐ 요점은 「두 번 본다」가 아니라 **「같은 눈이 두 번 보지 않는다」**입니다.
  제가 「이상 없음」이라 적었다고 u2 차례를 건너뛰지 마십시오.

```bash
node scripts/check-klifemap-health.mjs
```

---

## 4. 🔴 이 저장소에서 «하나 고치면 다른 게 망가지던» 문제 — 검사로 막아 두었습니다

사장님: **「하나 새로 하면 왜 다른 게 이상해지거나 사라지거나 개판이 되나?
구조적으로 그러지 않게 전체 코드를 다시 검토하던지 방법을 찾아」**

오늘 그 답으로 검사 셋을 새로 지었습니다. **내기 전에 반드시 돌립니다.**

```bash
cd ~/Documents/GitHub/klifemap
npx jest tests/page-has-required-parts.test.js          # 머리띠·꼬리말·i18n·짝맞음·단추 핸들러
npx jest tests/inline-script-syntax.test.js             # 인라인 스크립트가 문법으로 성한가
npx jest tests/korean-only-products-never-leak.test.js  # 성명학이 다국어로 새나
npx jest tests/dictionary-falls-back-key-by-key.test.js # 사전이 낱말 단위로 이어받나
```

### 4-1. 겪어서 아는 것 — **이것을 모르면 또 같은 사고가 납니다**

```
⛔ </body> 를 앞에서부터 찾지 않는다 — «주석 안»에 </body> 라고 적힌 곳이 있다.
   거기에 끼워 넣어 2,193줄짜리 스크립트가 통째로 잘렸다(saju.html). lastIndexOf 를 쓴다
⛔ 검사가 «주석에 적힌 이름»을 있는 것으로 세지 않게 한다 — 내가 지은 첫 검사가 그랬다.
   읽을 때 HTML 주석과 JS 여러 줄 주석을 먼저 지운다. 「검사가 틀리면 없느니만 못하다」
⛔ 한글 함수 이름 뒤에 \b 가 안 붙는다 — 한글이 \w 가 아니라서다. \s*\( 로 찾는다
⛔ 이 저장소 HTML 은 줄끝이 CRLF 다 — 여러 줄 정규식에 \r?\n 을 넣는다
⛔ Git Bash 가 heredoc·node -e 안의 역슬래시를 먹는다 —
   정규식이 든 스크립트는 «편집 도구로 파일에 적고» 실행한다
⛔ 셸 명령줄에 백틱이 든 글을 올리지 않는다. 오늘 그것으로 두 번 사고가 났다
```

---

## 5. 말(i18n) 구조 — **여기가 제일 자주 새는 곳입니다**

```
window.PAGE_I18N[lang]     지면마다의 사전
window.사전고르기(lang)     ⭐ 낱말 단위로 이어받는다 (오늘 고쳤다)
window.정해진말()           ⭐ 지금 말이 무엇인가의 «단일 출처». localStorage 를 직접 보지 않는다
T() (public/i18n-t.js)      <head> 에서 읽힌다
```

⭐ **언어 고르는 자리는 `<select>` 가 아니라 `<a data-lang>` 입니다.** 누르면 지면이 옮겨갑니다.
  저는 이것을 `<select>` 로 알고 한 차례를 통째로 잘못 쟀습니다. **눌러서 재십시오.**

⭐ 오늘 스페인어 새는 것을 **12% → 0.2%** 로 내렸습니다. 다섯 말을 다 눌러서 쟀습니다.

### 5-1. ⛔ 성명학 — **다섯 군데 문을 다 닫아 두었습니다. 열지 마십시오**

```
billing/billing.js   한국어에서만[] · pricingForDisplay() · subscriptionScopeLabels 거르개
server.js            /api/expert/quote 가 ko 가 아니면 404
public/i18n-chrome.js  한국어전용걷기() — [data-ko-only] 를 DOM «에서 지운다»(숨기는 게 아니다)
pricing.html         물어볼것 이 services[key] 존재까지 본다
tests/korean-only-products-never-leak.test.js  ← 잠가 둔 검사
```

---

## 6. 오늘 손댄 지면 — **상태와 남은 것**

| 지면 | 오늘 한 것 | 남은 것 |
|---|---|---|
| `pricing.html` | 점성학/싼상품/명리 세 덩어리로 나눠 «온 곳»에 따라 앞세움 | — |
| `tarot.html` | 12장 늘어놓기 → **누를 때마다 한 장씩** · SVG 그림 · 세 장 각각+종합 설명 | 결제 관문 뒤쪽 실측 |
| `liuyao.html` | 단추가 왜 꺼졌는지 «글로» 알려 줌 · 주사위 여섯 굴리는 애니메이션 | — |
| `saju.html` | 제가 끊어 놓은 스크립트를 되살림 | — |
| 12개 지면 | 사라졌던 머리띠·꼬리말을 되살림 | — |

---

## 7. ⬜ 넘기는 미완 — **숨기지 않고 다 적습니다**

```
🔴 결제 실키 전환 (0절) — 매출 0 의 원인. 첫날 일입니다
🔴 네이버·카카오 client secret 이 없어졌습니다. 개발자 콘솔에서 다시 발급받아 넣습니다
   ⭐ 콘솔 로그인은 «직접» 합니다 — 사장님: 「다 알아서 로그인해서 해. 자동로그인 되잖아」
   크롬은 9222 로 붙습니다. 포트가 없으면 내가 닫고 다시 띄웁니다(사장님이 허락하셨습니다)
⬜ 구글 로그인은 오늘 살렸습니다 — GCP 의 «승인된 자바스크립트 원본»이 비어 있던 것이었습니다
   ⚠ 리디렉션 URI 만 보고 「돼 있다」고 읽지 마십시오. 원본(origins)이 따로 있습니다
⬜ 1번이 놓고 오는 KLifeMap 몫 «전부» — 7-9 절에 표로 적었습니다
⬜ 점성학 다국어 콘텐트 — 사장님이 「점성학에 집중」하라 하셨고 시장이 제일 큽니다
```

---

## 7-9. 🔴 1번이 하던 KLifeMap 몫도 **전부** 이리로 옵니다

> 사장님 (2026-09-12 저녁): **「1번에 케이컬쳐 넘겨」**
> **「1번이 맡고 있던 케이라이프맵도 2번에게 다 넘기고」**

⛔ 「콘텐트만」이 아닙니다. **「다」**라고 하셨습니다. 1번은 이제 **KCW 하나**만 합니다.

| 1번이 놓고 오는 것 | 지금 상태 |
|---|---|
| 명리 상식 | 16편(1,200자 이상) — **미완** |
| 궁합 격국조합 | 28편 — **미완** |
| 다국어 정리 | 사전이 모자란 지면. ⛔ 성명학은 다국어에서 영구 제외 |
| 검색 색인 재확인 | 표본이 매번 무작위라 비교가 안 됐습니다. **고정 목록을 저장해 두고 재십시오** |
| 이름 DB | 2008~2025 채워짐 확인됨. 남은 갈래만 |

⛔ **접는 것** — 외국어 질문 수집(Reddit · 知乎 · 百度 · Yahoo!).
  8/14 부터 0건이고 두 번 재확인해도 0입니다. 없는 우물을 계속 긷지 않습니다.
  **「재 보고 안 된다고 적는 것도 결과다.」**

⭐ 그래서 u2 자리는 **KLifeMap 하나에 집중**합니다 —
  판정상품(사주·궁합·택일·성명학·점성학·타로·육효·손금·관상) · 결제 · 다국어 · 콘텐트 · 색인 전부.

---

## 8. ✅ u2 가 할 일 — **읽고 끝이 아닙니다**

```
1. 결제부터 봅니다 — node scripts/check-klifemap-health.mjs. 그 다음 0절의 길로 실키 전환
2. 2절(사장님이 정하신 것)을 자기 기억에 «원문 그대로» 넣습니다. 요약하지 않습니다
3. 09·15·21·03 결제 점검을 자기 자리에 겁니다 — 예약은 세션 안에서만 삽니다
4. docs/세션간-메모.md 에 「받았다」를 적습니다
5. 무엇이든 내기 «전»에 4절의 검사 넷을 돌립니다
```

⚠ **5번이 감수합니다. 배포 «전»에 봅니다.** 결론 없이 자료만 낸 것은 되돌립니다.

⭐ 그리고 한 가지 — 이 서비스는 **사람이 자기를 이해하려고 오는 곳**입니다.
  판정을 피하지 않습니다. 「자료를 보십시오」는 답이 아닙니다.
  **「N명 중 M명이 그랬다」**가 답이고, 정하는 것은 손님입니다.

---

*작성 5번(총괄) · 2026-09-12*
