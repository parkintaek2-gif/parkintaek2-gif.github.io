# 3번 콘텐트 예약 — 8/17(일) ~ 8/20(수)

> 사장님 지시(2026-08-16) — **「최소한의 일만 해라」 · 「수요일분 콘텐트까지 예약만 해라」**
> ⛔ **미리 만들지 않는다.** 이 문서는 «언제 무엇을»만 못 박는다.
> ⛔ 새 자료를 받지 않는다. **이미 손에 있는 자료**로만 짠다(받는 데 시간이 가장 많이 든다).
> ⛔ 하루에 **한 덩어리**만. 지면 한 절 또는 카드 한 벌 또는 영상 한 편.

## 예약표

| 날 | 무엇 | 자료(이미 있는 것) | 댈 지면 |
|---|---|---|---|
| **8/17(일)** | 카드 「나이띠로 본 월급과 근속」 6장 | `age-axis.json` | `/age` |
| **8/18(월)** | 지면 한 절 — `/elementary` 에 **설립 해로 본 흐름**(언제 학교가 많이 생겼나) | `elementary.json` 설립연 | `/elementary` |
| **8/19(화)** | 영상 「전국 평균이 두 개입니다」 14초 | `pages-university.json` | `/university` |
| **8/20(수)** | 카드 「직원을 두고 몇 해를 갔나」 6장 | `business-lifespan.json` | `/age` 또는 새 절 |

## 날마다 지킬 것 — 이미 자로 걸려 있다

```
① 자가시험을 통과해야 커밋한다
② 고침 → **커밋** → 빌드/렌더 → 지면 열어 확인   (순서를 바꾸지 않는다)
③ 카드·영상은 «.근거.json» 을 달고, 수는 전부 지면을 대야 한다
   node scripts/check-100y-content-evidence.mjs
④ 만든 카드는 그 지면에 **걸어 둔다** (HundredCardnews)
⑤ 영상은 videos.json 에 줄을 넣어야 /video 에 실린다
```

## ⛔ 이 나흘 동안 하지 않을 것

```
· 새 데이터 문 열기 (지역별 자격 15040841 은 「못 열었다」로 대장에 남겨 뒀다)
· 지면을 여러 장 새로 만들기 — 한 장씩만
· 검사자를 새로 만들기 — 있는 것으로 잰다
```

## 남아 있는 빚 (급하지 않다)

```
⬜ 대학알리미 3종 활용신청 — 포털 로그인이 필요해 못 한다
⬜ 커리어넷 열쇠 — 2번께 여쭀고 답 대기
⬜ 지역별 자격(15040841) 인자 — 포털 명세부터 받아야 한다
```
