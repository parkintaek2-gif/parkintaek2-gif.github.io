# 비상벨 — 버그가 생기면 바로 조치하는 체계

> 사장님 지시(2026-08-31): *"어떤 버그가 생길 때 바로 조치할 수 있는 비상벨 같은 걸
> 만들어봐. 만든 뒤 모든 유닛이 활용할 수 있게 해."* — 1번이 klifemap 몫으로 처음 만들고,
> **전 유닛이 같은 틀로 자기 사이트에 복사**해 쓰는 것을 목표로 한다.

## 핵심 — 새 알림 채널을 안 만들었다

klifemap `server.js` 의 `POST /api/owner/notify` 가 **이미 6유닛이 함께 쓰는 창구**다
(코드 주석: "여섯 유닛이 저마다 다른 셸에서 부를 창구다"). 사장님 메일로 즉시 간다.
비상벨은 이 창구를 **자동으로, 사람이 안 봐도 부르는** 감시기를 얹는 것뿐이다.

```bash
curl -s https://klifemap.ai/api/owner/notify \
  -H "Content-Type: application/json" -H "x-admin-key: <SAJU_ADMIN_KEY>" \
  -d '{"title":"제목(영문 필드명 title/text/from 을 쓴다 — 한글 필드명은 셸 인코딩으로 깨진 전례가 있다)","text":"내용","from":"어느 유닛"}'
```
⛔ **`제목`·`내용`·`보낸이`(한글 필드명)도 서버는 받지만, 셸(Git Bash 등)을 거치며
인코딩이 깨져 "제목이 없습니다"가 난 전례가 실제로 있다. 반드시 영문 필드명을 쓴다.**
하루 한도 30건(넘으면 429). 급하지 않은 것을 자꾸 부르면 진짜 비상이 묻힌다.

## 자동 감시기 — `tools/check-emergency.mjs` (klifemap)

klifemap.ai 의 **"죽으면 매출이 바로 멈추는 자리"만** 골라 재고(홈·서버헬스·결제견적·
토스상태·체크아웃화면·사주화면), 하나라도 깨지면 ① 위 알림창구를 부르고 ② 이 파일에
아래 형식으로 한 줄 남긴다.

```
node tools/check-emergency.mjs           # 한 번 재고 끝낸다
node tools/check-emergency.mjs --조용히  # 정상이면 조용히(크론용)
```

⭐ **다른 유닛도 이 파일을 그대로 복사해 `BASE`(자기 사이트 주소)와 `검사목록`
(자기 사이트의 "돈 걸린 핵심 경로")만 바꾸면 된다.** 콘텐츠 지면·부가 기능은 넣지
않는다 — 전부 재면 느려지고, 느리면 아무도 안 돌린다.

⚠ **처음 만들 때 도장(거짓경보)을 하나 잡았다** — 결제 견적·토스상태 API는
`x-admin-key`가 아니라 `x-api-key`(손님 열쇠)가 필요한데 몰라서 401을 "사이트가
깨졌다"로 잘못 알릴 뻔했다. 새 감시기를 만들 때는 **반드시 정상 상태에서 먼저
초록불을 확인**하고, 그다음 일부러 하나를 깨뜨려 빨간불이 실제로 뜨는지도 확인한다.

## 로그 규칙

- 이 파일은 **누적 로그**다 — 지우지 않는다. 새 사고는 맨 아래에 덧붙인다.
- 형식: `## 🔴 <사이트> — <YYYY-MM-DD HH:MM>` + 무엇이 깨졌는지 줄마다.
- 해결되면 그 항목 아래에 `✅ 해결 <시각> — <누가 무엇을 고쳤나>`를 한 줄 더한다.
- 자가시험(일부러 깨뜨려 본 것)은 로그에 안 남긴다 — 진짜 사고만 남겨야 이 파일이
  "지난번에 몇 번 났나"를 보여주는 진짜 기록이 된다.

## 각 유닛 체크리스트에 추가할 한 줄

기존 2시간 점검·매시 소통 루틴에 이 한 줄만 추가한다:

```
☐ 비상벨 — node tools/check-emergency.mjs(자기 사이트용)를 돌린다.
   빨간불이면 이 파일에 이미 기록됐는지 보고, 내 담당이면 바로 고친다.
```

## 🔴 kculturewire — 2026-09-01 11:19
- ⛔ 한글 누출 — https://www.kculturewire.com/ 화면에 「세종」가 보인다. 우리 손님은 영어권이다
- ⛔ 한글 누출 — https://www.kculturewire.com/most-read 화면에 「세종」가 보인다. 우리 손님은 영어권이다

### ⚠ 위 kculturewire 빨강 2건은 **거짓경보였다** — 2026-09-01 20:2x · 5번

새로 만든 `tools/check-emergency-kcw.mjs` 를 처음 돌리자 홈과 `/most-read` 둘 다
「한글 누출」로 빨강이 떴다. 실물을 열어 보니 **푸터의 법정 표기**였다 —

```
Published by KLifeDesign InC. · Business reg. 456-87-03384 ·
Mail-order licence 2026-세종-0591 (세종특별자치시)
```

통신판매업 신고번호와 사업장 주소는 **한글이어야 하는** 자리다. 빼면 법을 어긴다.

⇒ 자를 고쳤다 — 한글 누출을 볼 때 `<footer>` 를 셈에서 뺀다.
⛔ 한글 낱말을 하나씩 허용목록에 넣는 쪽으로 가지 않았다. 그러면 목록이 자라면서
   진짜 누출까지 조용히 통과시킨다.

⭐ **「모든 지면이 빨강」이 자의 흠이라는 신호였다.** 극단값이 나오면 발견으로 보고하기
전에 도구 결함을 먼저 찾는다 — `docs/비상벨.md` 가 이미 「반드시 정상 상태에서 먼저
초록불을 확인하고, 그다음 일부러 깨뜨려 빨간불이 뜨는지 본다」고 못박아 둔 그대로다.
고친 뒤 초록 9개 확인 · 자가시험(홈을 없는 주소로) 빨강 1건 확인.

### 유닛별 비상벨 자리 — 2026-09-01 20:2x 실측 (사장님 「모든 세션 잘 작동하는 지 확인하라」)

```
1번 klifemap.ai       klifemap/tools/check-emergency.mjs             ✅ 초록 (6개 전부 정상)
3번 100yearmap.com    dataeconomics/tools/check-emergency-100yearmap.mjs  ✅ 초록 (6개)
5번 kculturewire.com  dataeconomics/tools/check-emergency-kcw.mjs    ✅ 초록 (9개) ← 오늘 신설
6번 seoulmarkets.com  dataeconomics/tools/check-emergency.mjs        ✅ 초록 (4개)
2번 · 4번 · 7번        지킬 «사이트»가 따로 없다 — 보조·조율 자리다
```

⚠ **5번 몫이 8/31 지시 뒤 열흘 가까이 비어 있었다.** 남이 만든 틀을 「좋다」고 적어 두는
것과 «내 자리에 세우는 것»은 다른 일이다. 4번이 이것을 메모로 짚어 주어 알았다.
⚠ 4번이 「3번도 없다」고 적었는데 **3번 것은 있다**(`check-emergency-100yearmap.mjs`).
파일 이름이 `-100yearmap` 접미사라 `check-emergency.mjs` 만 찾으면 안 보인다.

🔴 **아직 못 한 것 — 넷 다 «손으로» 돌려야 한다.** 크론에 안 물려 있다.
   비상벨은 사람이 기억할 때만 도는 것이면 비상벨이 아니다. 이어서 크론에 물린다.
