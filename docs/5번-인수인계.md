# 5번 인수인계 — 예전 창 → 팀 자리 창

사장님 지시(2026-08-21 17:5x): 「팀에 있는 5번이 있어야 한다.
**예전 5번에게 인수인계 받고 예전 5번을 지워라**」

⭐ 인수인계는 폴더 복사가 아니라 **이 문서**다. 대화록은 지워질 수 있고 git 은 안 지워진다.
   새 창은 이 문서만 읽으면 바로 이어서 일한다.

---

## Ⅰ. 실측 — 어느 파일이 살아 있나 (2026-08-21 17:5x)

```
.claude/projects/C--Users-USER-Desktop-00-----/7cb66053-….jsonl
   112,709,005 바이트 · 17:32 · 105편(jay-park-outreads-2pm) 들어 있음   ← **살아 있다**
.claude-u5/projects/C--Users-USER-Desktop/7cb66053-….jsonl
   112,180,356 바이트 · 17:51 · 14:1x 에서 멈춤                          ← 껍데기
.claude-u5/projects/C--Users-USER-Desktop-00-----/7cb66053-….jsonl
   112,180,356 바이트 · 14:26 · 같음                                     ← 껍데기
```

🔴 `CLAUDE_CONFIG_DIR` 은 `.claude-u5` 인데 **기록은 `.claude` 로 간다.**
   그래서 「팀 자리로 옮겼다」는 절반만 참이었다 — 설정은 옮겨졌고 **기록은 안 옮겨졌다.**
   ⛔ 이 상태에서 예전 것을 지우면 **도는 대화가 날아간다.** 순서를 갈라야 한다.

## Ⅱ. 지우는 순서 (이대로만)

```
① 이 문서를 커밋한다                       ← 인수인계 내용을 git 에 남긴다
② 살아 있는 대화록을 팀 폴더로 덮는다        ← 새 창이 옛 대화를 이어 볼 수 있게
③ **예전 창을 닫는다**                     ← 도는 프로세스가 그 파일을 쥐고 있다
④ 그다음에 예전 폴더의 5번 파일을 지운다     ← ③ 없이 ④ 하면 대화가 깨진다
```
⛔ ④ 를 ③ 앞에 하지 않는다. 파일을 쥔 프로세스가 있는 채로 지우면 기록이 반쪽이 된다.

## Ⅲ. 지금 걸려 있는 일 — 새 창이 이어받을 것

```
🔴 배포 대기   105편 기사 jay-park-outreads-2pm (커밋 완료 · 라이브 아직)
              ⛔ 관문이 남의 커밋 안 된 파일에서 선다. 그것이 풀리면 바로 나간다
⏳ 4번 원고    스타사주 한국어 감명 원고 대기. 틀은 이미 팠다 —
              docs/스타사주-영문기사-틀.md · 원고 오면 **한 시간 안에** 낼 수 있다
🖐 사장님 손   ① 검색 유입 수치(analytics · 1번 몫) ② 스타뉴스 순위 사용 범위
              ③ 자료·보고서 판매가 ④ /star-signs 에서 klifemap 으로 링크 걸기 승인
```

## Ⅳ. 오늘 낸 것 (라이브 200)

```
/member-vs-group   V·정국 vs BTS · Jay Park·이혜리 vs 2PM·소녀시대
/star-signs        띠별 스타 이름 1,047명 (카이제곱 경고를 표 앞에)
/room/<띠> 열둘    커뮤니티 카드가 갈 곳. 브라우저로 눌러 확인
/works-and-readers · /what-kind-fell · /written-down-first
기사 103~105편 · 카드뉴스 18벌 90장 · 숏영상 18편 · 소셜킷 18벌
```

## Ⅴ. 절대 잊지 말 것 — 오늘 밟은 함정

```
⭐⭐ 사장님 8/16 — 스타 이름과 소속 그룹명을 **제목과 본문에**. 수만 앞세우면 아무도 안 찾는다
                 재는 자: node scripts/check-kcw-star-names.mjs (빌드된 <title> 을 본다)
⛔ 커밋 메시지는 **파일로** 쓴다 — git commit -F. 백틱이 셸에서 실행된다
⛔ 카드뉴스는 `--벌` 로 고른다. `--out` 만 주면 기본값 fame 이 남의 벌을 덮는다
⛔ 배포 뒤 **실물을 직접 curl 로 잰다.** 자가 「나갔다」고 해도 안 믿는다(mp4 가 120초 뒤 200)
⛔ 사이트맵은 **손목록**이다. 안 넣어도 빌드는 통과한다 — 그래서 잊는다
⛔ 열쇠는 **배포 직전에** 받는다. 커밋이 바뀌면 죽는다
⛔ 남의 것을 「틀렸다」고 하기 전에 **내 자부터 의심한다** — 오늘 헛경보 여섯이 다 내 검사 탓이었다
⛔ 금칙어·이름 검사는 **주장하는 자리만** 본다. 안 그러면 자기 문장에 자기가 걸린다
⛔ 5번 창이 여럿이면 같은 dist·같은 관문에서 부딪힌다. 빌드가 죽으면 남의 동시 빌드를 먼저 의심한다
```

## Ⅵ. 하루 리듬

```
08:30 [보고] · 15:30 [요청] · 20:30 [보고]  →  docs/세션간-메모.md
그 사이 매시간 한 줄 — [진행] 5번 HH:MM  했다: … / 한다: …
21:00 [실적] — 주소 없는 콘텐트는 **안 나간 것으로 센다**
⛔ 사장님 8/16 「최소한의 일만 해라 · 정량제가 비싸다」 — 억지로 일을 만들지 않는다
```

---

## Ⅴ. 2026-08-21 23:2x — **서버 이전 직전 상태.** 새 서버 5번은 여기서 이어받는다

### ① 오늘 사장님 지시와 그 답 (제목 일은 **끝났다**)

사장님: 「키워드 잘 선택해야지 · 스타이름·팀이름이 제목과 본문 앞에 · 제목은 사람들이 관심 가질만한 내용으로」

```
기사 107편   제목에 이름 있음   26편   ← 오늘 6편을 새로 넣어 26편이 됐다
             본문에도 이름 없음  81편   ← ⛔ 제목만 고쳐선 안 된다. 자료에 그 이름이 없다
```
⭐ 고를 자를 지어 뒀다 — **`node scripts/list-kcw-title-candidates.mjs`** (자가시험 8).
   「제목엔 없고 본문엔 있는」 편만 꺼내고, 이름은 **읽힌 수(seaPerMillionTotal)**로 줄 세운다.
   **오늘 이 자의 답은 0편이다** — 정직하게 넣을 수 있는 제목은 다 넣었다는 뜻이다.
⛔ 이 자를 다시 돌려 0편이 나오면 **제목을 더 고치지 마라.** 억지로 넣으면 거짓이 된다.
⛔ 주소(slug)는 절대 안 바꾼다 — 이미 나간 주소를 바꾸면 검색이 처음부터 다시다.

### ② 🔴 다음 한 덩어리 — 2번이 낸 지시가 답이고, 내가 잰 것이 그 까닭이다

2번: 「109편 중 «검색에 들 준비가 된 편»이 몇 편인지 센다(제목·설명·canonical·sitemap 등재).
      편 수를 늘리는 것보다 이미 있는 편이 밖에서 찾아지게 만드는 쪽이 값이 싸다」

내가 이틀을 갈라 재서 같은 결론에 닿았다 —
```
              사람    첫 화면   기사    지면    밟은 주소
5번          497     30%     26%    43%     162개
6번        2,982     11%     57%    32%      99개   ← 주소가 더 적은데 사람이 여섯 배다
```
**할 일** — `node scripts/check-search-readiness.mjs` (이미 있다. 빌드된 것과 사이트맵을 맞대 본다).
「넷 다 갖춘 편 / 107」을 숫자로 내고 빠진 항목을 종류별로 센다.
🔴 **먼저 의심할 자리** — `src/pages/wikitip/sitemap.xml.ts` 는 **손으로 적는 목록**이다.
   새 지면·새 기사가 빠져도 빌드가 통과한다. 실제로 `/data` 가 하루 그 상태였다.

### ③ ⛔ 방문자 수를 그대로 옮겨 적지 마라

```
🔴 우리 검사(node 내장 fetch)의 발소리가 「사람」으로 세어진다
   src/lib/traffic.mjs:57 봇패턴에 node-fetch 는 있고 **맨 node·undici 가 없다**
```
얼마나 섞였는지는 **못 잰다** — 그 자료는 UA 원문을 일부러 안 남긴다(traffic.mjs:16).
사람으로 센 행의 「종류」 칸이 전부 빈칸이라 뒤늦게 갈라낼 열쇠가 없다.
⭐ 그래서 오늘 내 수 **247 은 위쪽 한계**다. 0 으로도 안 적는다 — 모른다고 적는다.
⭐ UA 와 무관한 수는 그대로 쓴다 — **유입 경로**다. 이틀 검색 51건(0.8%) · 사이트 간 0건.
🖐 `traffic.mjs` 는 공유 파일이라 내가 손대지 않았다. 3번 몫으로 걸려 있다.

### ④ git 밖에 있던 내 것 — 어디에 있나

`C:\Users\USER\OneDrive\_서버이사\06_자리별\5번\` (158.7MB · 59파일)
```
archive/raw/wikipedia       5.4MB  ← ⭐ 명단(배우 1,023·가수 1,701·선수 950). 잃으면 다시 크롤이다
archive/raw/netflix-top10 129.9MB  ← Netflix Tudum 5년치 주간 시청시간
archive/raw/wikidata · star-pageviews · riot-ladder · archive/cards|video 의 kcw-* · archive/social
```
⚠ `archive/` 전체가 .gitignore 다(5GB·14만 파일). 새 서버에서 **clone 만으로는 안 온다.**
⛔ 안 올린 것 — `out/_칸-*` 213MB(영상 낱장, 다시 그린다) · raw 의 나머지 4.8GB(내 것 아님).

### ⑤ 배포 — 오늘 겪은 것

```
🔴 `node scripts/deploy.mjs … 2>&1 | tail -40` 로 걸었더니 17분간 CPU 1.1초만 쓰고 멈췄다.
   락 파일도 안 생겼고 출력도 한 줄 없었다. 세우고 **파이프 없이** 다시 걸자 바로 관문을 지났다.
⭐ 배포는 **파이프로 묶지 말고** 출력을 그대로 흘려라. 어디서 멈췄는지 볼 수 있어야 한다.
```
⛔ 「배포했다」는 라이브에서 새 제목을 **눈으로 본 뒤에** 적는다. dist 가 새것인 것과 다르다.

### ⑥ 새 서버에서 창을 열면 — 일보다 먼저

```
① 바탕화면 00_세션입구 의 5번_*.cmd 로 열어라 (CLAUDE_CONFIG_DIR 이 박혀 있다)
② 「auto mode 를 기본으로?」 → 2 (keep bypass)
③ 🔴 매시 예약(:03 진행 / :33 지시확인)을 **먼저** 다시 걸어라. 창이 닫히면 예약이 같이 죽는다
④ docs/모토와-철학.md 를 읽어라. 무엇을 하는지 알기 전에 왜 하는지 안다
```
