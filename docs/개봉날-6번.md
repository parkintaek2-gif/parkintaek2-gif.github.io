# 개봉날 차례표 — 6번(SeoulMarkets) · **2026-08-15 아침**

> 2번 지시(81245) — *「그날 아침에 켤 것을 그날 정하면 반드시 빠뜨립니다. ③(되돌리는 것)이 제일 중요합니다.
> 6번은 ②에 **애드센스 상태**를 꼭 넣으십시오 — 넷 중 광고가 처음 열릴 자리가 6번입니다」*

⛔ 계획서가 아니다. **그날 아침에 그대로 따라 하는 차례표**다. ⛔ 오늘 켜지 않는다. 적어만 둔다.

---

## 🔴 먼저 — **6번은 「켤 스위치」가 거의 없다. 정직히 적는다**

B2B(기관에 자료 파는 것)는 **이미 라이브**다 — 기사 45편 · `/data` 카탈로그 7상품 · 「Request the dataset」 문의폼(샘플+약관 이메일). 손님이 오늘도 살 길이 있다.
⛔ 즉시 **결제(체크아웃)는 ▨** — klifemap 계좌 콘솔 넉 줄(🖐 사장님)이 켜져야 전 유닛이 같이 열린다. 6번 코드로 켜는 게 아니다.
⭐ 그러니 6번 개봉날은 「스위치 켜기」가 아니라 **「닿는 길이 다 살아 있나 재고, 광고 상태를 아는 것」**이다.

---

## ① 켜는 것 — **짧다**

| # | 무엇 | 누구 | 어떻게 |
|---|---|---|---|
| 1 | **커밋 먼저** — 나간 것 중 커밋 안 된 것 0 | 6번 | 공유 워킹트리라 창 죽으면 통째로 사라진다. ⛔ `git add -A` 금지·경로만 |
| 2 | 계좌 콘솔 넉 줄 (전 유닛 공통 결제) | 🖐 **사장님** | 하나만 없어도 결제 501. 이게 6번 B2C의 유일한 문 |
| 3 | 애드센스 동의 메시지(CMP) 마무리 | 🖐 **사장님** | 계정 설정+법무 결정이라 6번이 못 한다 |
| 4 | 라이선싱 문의폼이 **실제 수신 메일로 닿나** 마지막 확인 | 6번 | 폼 → `sibcheongan@gmail.com` 도착까지 한 번 |

```
남이 해 줘야 하는 것 — 🖐 사장님 둘 (② 계좌 · ③ 애드센스 CMP)
```
**배포는 자물쇠 순서로만** — ⛔ 그날 줄이지 않는다 · ⛔ ctype 직접 호출 안 한다(2번 몫, 커밋·푸시까지만)

---

## ② 재는 것 — **켠 뒤 바로. 🔴 ①은 애드센스다**

```
1  🔴 애드센스 상태 — 「게재 중」인가 「준비 중」인가   🖐 콘솔(사장님 계정)
      · 「준비 중」이면 **그날 광고 매출 0**. 그날 아침에 알아야 한다. 그날 알아보면 늦다
2  seoulmarkets.com/ · /data · 기사 한 편        각 200
3  /data 상품면 「Request the dataset」 폼이 보이고 → 문의 메일이 닿나   폰 375px
4  표가 폰에서 안 넘치나 (오늘 고친 것)          /data/target-price-accuracy 를 375px 로
5  KLifeMap 문 라이브 · 딱지 `?from=smk&at=…` 그대로 · 문구가 「daily reading」 아닌가
6  색인 — 기사 noindex 0 · Search Console 크롤 오나   🖐 (사장님 계정)
```
⛔ 「배포했다」로 ✅ 를 치지 않는다. **라이브에서 다시 재야** ✅ 다.

---

## ③ 🔴 되돌리는 것 — **제일 중요하다. 창이 죽어도 이 줄만 있으면 된다**

```
무엇이 잘못되면            어떻게 되돌리나
──────────────────────────────────────────────────────────────
지면이 깨졌다              git revert <그 커밋> · git push · 2번이 배포
                          ⛔ git reset --hard 금지 — 공유 워킹트리라 남의 커밋이 같이 날아간다
라이브에 틀린 숫자          ⛔ 조용히 고치지 않는다. frontmatter `corrections` 에 적고 고친다
                          (우리가 파는 것은 숫자의 신뢰다. 고친 자국이 신뢰다)
KLifeMap 문이 이상하다     그 문 커밋만 revert(klm-door 블록 제거). ⛔ 딱지 측정만 빠지고 지면은 산다
애드센스가 「준비 중」이다   되돌릴 게 아니다 — 🖐 사장님께 알린다(광고 0을 그날 아침에 알기)
무엇이 나갔는지 모르겠다     git log --oneline -10 · 그리고 **라이브를 연다** — 저장소가 아니라 화면이 참이다
창이 죽었다                ⭐ 창이 열리면 **가장 먼저 매시 예약을 다시 건다** · 그다음 이 문서
```
**되돌릴 수 없는 것 — ⛔ 미리 안다**: 🖐 사장님 계정 작업(애드센스·Search Console)은 우리가 못 되돌린다.

---

## 그날 아침 순서 한 줄로

```
커밋 → ② 여섯 줄 재기(🔴 애드센스부터) → 틀린 게 있으면 ③ 으로
      → 6번이 켤 코드 스위치는 없다. 🖐 사장님 계좌·CMP 가 켜지면 결제·광고가 열린다
```
