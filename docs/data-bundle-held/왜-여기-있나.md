# 이 세 표는 왜 웹에서 내렸나 (2026-09-02)

`docs/데이터-라이선스-대장.md` 가 2026-08-08 에 이렇게 못박아 두었다.

```
| 〃 **파일로 파는 것** | 한 벌 CSV | — | **미확인. 확인 전에는 안 연다** | 🔴 |
/data      값도 안 적고 파일도 안 준다. 지면에 그 까닭을 적어 두었다      → 이미 그렇게 나갔다
```

⛔ **그런데 그 사이 열려 있었다.** 2026-09-02 에 `check-product-bundle.mjs` 가 잡았고,
라이브를 재 보니 세 파일이 다 **200** 이었다 — `korean-titles-netflix-runs.csv` 는 769KB 다.

```
korean-titles-netflix-runs.csv   나라별 chart_places · peak_rank · 주간 범위
korean-titles-summary.csv        같은 것을 작품 한 줄로
korean-actors-netflix.csv        배우별 charting_titles · chart_places
```

세 표의 수는 **Netflix Tudum 주간 순위에서 나온 것**이다. 대장의 판정은 이렇다 —
집계(합·편수·비중)를 지면에 내는 것은 🟢, **원본에 가까운 표를 파일로 내주는 것은 🔴.**

## 다시 열려면 무엇이 있어야 하나 (대장 ④ 그대로)

```
① Tudum 지면이 실제로 렌더된 상태에서 읽은 이용 조건 (브라우저로 열어야 한다)
② 넷플릭스에 문의해 받은 서면 회신
③ 「집계는 되고 파일은 안 된다」를 우리가 스스로 긋고 그 선을 지면에 적기
   ⚠ 이것은 확인이 아니라 «우리 판단»이다. 판단으로 쓸 거면 판단이라고 적어야 한다
```

⚠ 2026-09-02 에 ①을 하려 했으나 **이 세션에서 브라우저(playwright MCP)가 붙지 않았다.**
   「없다」가 아니라 **「못 봤다」**다.

## 지우지 않고 여기 두는 까닭

표 자체는 우리가 가공한 것이고 다시 만들 수 있다. 그러나 **무엇을 내렸다가 왜 내렸는지**가
남아야 다음 사람이 같은 것을 또 열지 않는다. `public/` 밖이므로 손님에게 안 나간다.

⛔ 이 폴더를 `public/` 으로 되돌리지 말 것. 대장이 🟢 가 되는 날 저 검사가 저절로 풀린다.
