# 🚪 2번 세션 입구 — **새 창이 열리면 여기부터**

> 만든 날 2026-09-19 · 만든 자리 5번(총괄)
> 까닭 — 2번 세션이 로그인이 끊겨 하루를 쉬었고, 다시 들어왔을 때
> 「나는 누구이고 무엇을 하던 중인가」를 잇는 자리가 없었다. 그 자리를 여기 둔다.

---

## Ⅰ. 나는 누구인가

```
번호      2번
계정      admin@klifedesign.net   ⭐ 사장님과 «함께» 쓰는 계정이다
프로필    CLAUDE_CONFIG_DIR = C:\Users\USER\.claude-u2
맡은 일   SeoulMarkets 개발 (2026-09-18 사장님 지시로 옮겨졌다)
```

⚠ **팀 멤버 목록에 `u2@klifedesign.net` 이 없다.** 그것은 옛 계정이고 팀에서 빠졌다.
  「내 자리가 없어졌나」로 읽지 않는다 — 사장님이 직접 못박으셨다:
  「**admin 계정을 2번과 내가 쓴다고 했잖아?**」

⚠ **KLifeMap 콘텐트는 더 이상 내 몫이 아니다.** 2026-09-19 오전에 1번으로 옮겨졌다
  (사장님: 「1번한테 모든 사이트의 콘텐트를 만들라고 지시했다. 총 24개, 매일」).

---

## Ⅱ. 창을 열면 — **이 네 줄부터**

```bash
cd C:\Users\User\Documents\GitHub\dataeconomics
git pull --rebase origin main
grep -n "5번 → 2번" docs/세션간-메모.md | tail -5      # 나에게 온 말
git log --oneline --since="12 hours ago" | head -20    # 그 사이 무슨 일이 있었나
```

그 다음에 **지금 잡을 일**을 확인한다 — 아래 Ⅲ.

⛔ 일을 시작하기 전에 히스토리를 본다. 사장님 지시다 —
  「**뭘 지시하면 일단 히스토리를 봐**」. 「기획·설계」에만 걸린 규칙이 아니다.

---

## Ⅲ. 지금 잡을 일

| | |
|---|---|
| **F5 파일 배달 + 내려받기 지면** | 유료 손님이 산 자료를 CSV·Parquet 파일로 받아 가는 길 |
| 목표일 | **2026-09-23** — 「최소상품 판매 가능」이 이 날짜에 걸려 있다 |
| 설계 | `docs/서울마켓츠-실행계획-영문FnGuide-재편.md` Ⅲ장 |
| 그다음 | **F7 REST API(읽기 전용) + 문서 + 열쇠 · 10-03** |

무엇을 「됐다」로 볼 것인가 —

```
✅ 결제한 상품에 맞는 파일만 받아진다 (안 산 것은 못 받는다)
✅ 파일마다 «판 번호»가 박힌다 — 시세 판·재무 판
✅ CSV 는 UTF-8 BOM (엑셀에서 한글이 안 깨지게)
✅ 지면에서 «실제로 눌러» 받아 열어 본다. 200 만 보고 됐다 하지 않는다
⛔ 빈 봉투를 만들지 않는다 — 2026-09-15 에 「돈 내고 빈 봉투」 사고가 났다
⛔ 0 으로 채우지 않는다. 없는 값은 null 이다
```

**이미 서 있는 것은 다시 만들지 않는다** —

```
src/data/korea-financial-account-english.json   영문 계정 사전
src/data/korea-index-name-english.json          영문 지수명
src/pages/data/financials.astro · indices.astro · korea-valuation.csv.ts
archive/raw/dart-financials/ · archive/raw/indices/
```

---

## Ⅳ. 거르면 안 되는 것 — **바쁜 것은 까닭이 되지 않는다**

### 4-1. 🔴 KLifeMap 손님길 세 징검다리

```
차례   07:30 · 10:30 · 13:30 · 16:30 · 19:30   (내 몫. 나머지 시각은 5번이 잰다)
무엇   1. 들어오기 — 손님이 로그인·가입할 수 있나
       2. 돈 내기 — 결제창이 뜨고 눌러서 주문이 «실제로» 만들어지나
       3. 다시 보기 — 산 것을 다시 찾을 수 있나
⛔ 승인(capture)은 부르지 않는다. 카드번호를 넣지 않는다
⛔ 「단추가 보인다」를 「결제가 된다」로 읽지 않는다
✅ 잰 줄은 대장에 남긴다 — 줄이 없으면 배포가 잠긴다
```

사장님: 「**결제가 안되는 사이트는 정말 쓰레기야**」

### 4-2. 매시 소통

정각마다 5번과 소통한다. `docs/세션간-메모.md` 에 `## [2번 → 5번] 시각 제목` 꼴로 적고
커밋·푸시한다. **그것이 유일한 전달 통로다** — `ListAgents` 로는 서로 안 보인다.

---

## Ⅴ. 사고를 내는 자리 넷 — **여기만은 틀리면 회사가 멈춘다**

```
⛔ klifemap 에 ctype apply 금지
   콘솔 환경변수가 통째로 지워진다. 2026-09-11 에 그래서 구글·네이버 로그인이 사라졌다.
   KLifeMap 배포는 Cloudtype «대시보드»에서 한다

⛔ ctype 은 예외 없이 -t, 그리고 «서 있는 폴더»가 짝이어야 한다
   SeoulMarkets   dataeconomics 폴더 · -t @parkintaek2/seoulmarkets:main · 앱 이름 web
   KLifeMap       klifemap 폴더      · -t @parkintaek2/klifemap:main     · 앱 이름 klifemap-app
   친 뒤 나오는 줄을 읽는다 — 앱 이름이 다르면 그 자리에서 ctype remove 한다

⛔ ctype remove 는 커스텀 도메인 연결까지 가져간다
   지우기 전에 ctype routes 로 붙은 도메인을 적어 둔다. 여섯이어야 한다

⛔ 셸 명령줄에 백틱이나 한글 변수명이 든 글을 올리지 않는다
   셸이 그것을 실행해 엉뚱한 배포가 뜬 적이 있다. 글은 편집 도구로 파일에 적는다
```

---

## Ⅵ. 일하는 법 — 사장님이 정하신 것

```
✅ 선입선출 — 들어온 차례대로. 예외는 서버 다운·버그·급한 지시뿐
✅ 시각은 한국시간(KST). 이 PC 가 이미 KST 다 — toISOString() 금지, 9시간 더하기 금지
✅ 업무시간 07:00~21:00
✅ 돈이 직접 오가는 것(결제·입출금·환불·결제취소)만 승인받는다. 그 밖은 판단해서 실행
✅ 지시받은 일이 끝나면 사장님께 «직접» 메일 — parkintaek@naver.com
   node scripts/send-mail.mjs 로 스스로 보낸다. PDF·엑셀·워드로 (마크다운은 표가 깨진다)
✅ 만들면 «눈으로 보고» 완료라고 한다. 검사를 돌린 것으로 갈음하지 않는다
⛔ 승인 요청을 올리지 않는다. 선택지를 늘어놓지 않는다 — 정해서 실행하고 결과를 보고한다
⛔ 손님 이메일 주소를 저장소·로그 어디에도 적지 않는다
```

---

## Ⅶ. 더 읽을 곳

```
CLAUDE.md                                     이 저장소의 정본. 길지만 사고 사례가 다 있다
docs/서울마켓츠-실행계획-영문FnGuide-재편.md      내 일정과 관문
docs/세션간-메모.md                             유닛 사이의 전달 경로
docs/사장님-지시-대장.md                        지시 원문
docs/네-사이트-강령.md                          왜 이 일을 하는가
```

⭐ 판단이 갈리면 **「돈이 되나」가 아니라 「나침반이 되나」**로 정한다.
