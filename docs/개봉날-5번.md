# 개봉날 차례표 — K Culture Wire(5번)

**2026-08-15 개봉.** 이 문서는 그날 아침에 **위에서 아래로 그대로 따라 하는 것**이다.
⛔ 그날 새로 판단하지 않는다. 판단은 오늘(8/10) 해 두고, 그날은 **읽고 누르기만** 한다.

🔴 이 차례표는 **8/10 16:0x 에 라이브에서 24걸음을 실제로 눌러 보고** 쓴 것이다.
   적은 것이 아니라 **된 것**이다. 그때 21/24 였고, 막힌 셋은 배포가 안 된 것뿐이었다.

---

## ① 켜는 것 — 5번이 켤 스위치는 **없다**

```
사이트     이미 라이브다. 개봉일에 새로 켤 코드 스위치가 없다
광고       ⛔ **켜지 않는다.** Riot Production(App 866800) 승인 전이다.
           승인 전에 /esports 에 광고가 뜨면 **API 가 영구 취소**된다
결제       5번은 결제가 없다. 유료는 /subscribe 로 목록만 받는다
```
⭐ 그러니 개봉일 아침에 5번이 할 일은 **켜는 것이 아니라 재는 것**이다.

---

## ② 재는 것 — **이 순서로 누른다**

### ②-1 먼저 관문과 검사 (5분)

```
node scripts/check-wikitip-all.mjs      → **82개 전부 통과**라야 한다
node scripts/check-deploy-ready.mjs     → 「손님에게 나가는 것 중 커밋 안 된 것 없다」
```
🔴 검사가 하나라도 서면 **그것부터 고친다.** 개봉일에 빨간 검사를 안고 가지 않는다.

### ②-2 라이브 24걸음 (10분)

⛔ 브라우저로 하지 않는다. `node:http`/`node:https` 로 **Host 를 직접 넣어** 잰다
   (node fetch 는 Host 를 못 바꾼다 — 그래서 남의 사이트를 재게 된다).

```
첫 화면 /              기사 목록 /articles       방법 /about          정정 /corrections
연락 /contact          받아보기 /subscribe        자료 /data           RSS /rss.xml
사이트맵 /sitemap.xml   robots /robots.txt

갈래 넷   /section/stars · /section/titles · /section/industry · /section/tradition
옛 주소   /titles · /industry · /esports        ⛔ 살아 있어야 한다. 지우면 링크가 죽는다
새 지면   /provenance · /crowding · /zodiac
최신 기사 셋 (그날의 최신 셋으로 바꿔 넣는다)
권리자    /for-industry
```
**24걸음이 다 200 이라야 한다.** 8/10 실측은 21/24 였고 막힌 셋은 전부 **배포 대기**였다.

### ②-3 그날의 수 셋 (2분)

```
사이트맵 몇 장인가        8/10 실측 **759장**
기사 몇 편인가           8/10 **75편**
한국어가 새어 나오나       node scripts/check-english-only.mjs → 「한국어가 없다」
```
🔴 **사이트맵 장수가 줄었으면 멈춘다.** 늘거나 같아야 한다. 줄었으면 무엇이 빠졌는지 먼저 찾는다.

### ②-4 검색이 우리를 보나 (3분)

```
node scripts/check-crawlable.mjs        robots·noindex·canonical·응답 시간
node scripts/check-indexed-urls.mjs     색인에 잡힌 것 (대조군 먼저)
```
⚠ **색인이 적은 것은 흠이 아니다.** 8/10 기준 발견 718 · 색인 49 다.
   683장은 거절이 아니라 **크롤이 아직 안 온 것**이다. 개봉일에 748장이 다 서 있을 수 없다.
   ⛔ 그날 사이트맵을 다시 넣거나 색인 요청을 730장에 넣지 않는다. **기다릴 것**이다.

---

## ③ 되돌리는 것 — 무엇이 잘못됐을 때

```
수가 틀렸다        ⛔ 조용히 고치지 않는다. **정정문**을 앞말에 달고
                  `src/data/wikitip-page-corrections.json` 원인표에 **같이** 등록한다
                  (두 곳이 갈라지면 「그날 정정 몇 건」이 조용히 틀린다 — 8/10 에 겪었다)
지면이 깨졌다      git revert <그 커밋>   ⛔ `reset --hard` 금지. 남의 것까지 날아간다
빌드가 죽었다      **에러의 앞부분**을 본다. 8/10 에 libuv assertion 뒤에 내 JSX 실수가 숨어 있었다
자료가 옛 판이다    `node scripts/rebuild-kcw-data.mjs` — 자들끼리 서로 보므로 한 바퀴 더 돈다
창이 죽었다        ⛔ 제일 먼저 **매시 예약을 다시 건다.** 일보다 그것이 먼저다
```

⛔ **배포는 내가 하지 않는다.** 2번·7번 몫이다. 나는 커밋하고 「배포 대기」라고 적는다.

---

## ④ 🖐 사장님 손 — 개봉일까지 필요한 것

```
🖐 KOFIC/KOBIS 열쇠   영화 자료를 늘리려면 필요하다.
                     ⛔ **그것 때문에 멈추지 않는다.** 없어도 개봉은 된다
🖐 X 계정 그림 둘      @kculturewire 이름·소개·링크는 살아 있다. 그림만 남았다
                     ⚠ 크롬 창을 최대화해야 올라간다(2번이 겪음)
```
⚠ 그 밖에 5번이 사장님 손을 기다리는 것은 **없다.**

---

## ⑤ 그날 하지 않을 것

```
⛔ 광고를 켜지 않는다 (Riot 승인 전)
⛔ 사이트맵을 다시 넣지 않는다 (8/17 에 색인을 다시 잰다)
⛔ 색인 요청을 730장에 넣지 않는다
⛔ 새 기사를 급히 쓰지 않는다 — 있는 것이 제대로 서 있는지가 먼저다
⛔ 애드센스 화면을 누르지 않는다 — 심사가 처음으로 돌아간다
```

---

**8/10 16:0x 실측 — 24걸음 중 21. 막힌 셋(`/section/tradition` · `/zodiac` · 75편째)은
배포 대기일 뿐이고 코드 문제가 아니다.**
